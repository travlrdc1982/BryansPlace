<?xml version="1.0" encoding="UTF-8"?>
<survey
  alt="Typing Tool"
  autosaveKey="psid"
  browserDupes=""
  builderCompatible="1"
  compat="146"
  delphi="1"
  extraVariables="ipAddress,source,record,decLang,list,userAgent,md,comp_sflag"
  fir="on"
  html:showNumber="0"
  mobile="compat"
  mobileDevices="smartphone,tablet,desktop"
  name="Survey"
  remerged="20260217_01:38"
  secure="1"
  setup="term,decLang,quota,time"
  ss:disableBackButton="1"
  ss:enableNavigation="0"
  ss:hideProgressBar="1"
  state="testing"
  version="19">

<res label="sys_check-error-atLeast-sing-column">You have left question(s) unanswered. Please provide an answer.</res>
<res label="sys_noAnswerSelected">You have left question(s) unanswered. Please provide an answer.</res>
<res label="sys_survey.has-errors">You have left some questions unanswered. You will find the items that need you attention shown below.</res>
<res label="PROMPTERROR"><strong>You have left question(s) unanswered.  Are you sure you do not have an opinion?  If not, you can skip to the next question.</strong></res>
<res label="PROMPTERROR1"><strong>You have left question(s) unanswered.  Are you sure you do not have an opinion?  If not, you can skip to the next question.</strong></res>
<res label="PROMPTERROR2"><strong>You have left question(s) unanswered.  Are you sure you do not have an opinion?  If not, you can skip to the next question.</strong></res>

<samplesources default="0">
  <samplesource list="0">
    <title>Open Survey</title>
    <exit cond="qualified"><b>Thank you for completing the survey!</b></exit>
    <exit cond="terminated"><b>Thank you for your time.</b></exit>
    <exit cond="overquota"><b>Thank you for your time.</b></exit>
  </samplesource>
</samplesources>

<style name="respview.client.css" mode="after"><![CDATA[
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,400&amp;family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet">
<style>
/* --- page-level overrides --- */
body, .survey-body, .main-content {
    background: #F5F2ED !important;
}
.sc-native-hidden {
    border: none !important;
    box-shadow: none !important;
    background: transparent !important;
    padding: 0 !important;
    margin: 0 !important;
    min-height: 0 !important;
    overflow: visible !important;
}
.sc-native-hidden .question-text,
.sc-native-hidden .comment,
.sc-native-hidden .instruction-text,
.sc-native-hidden .answers-list,
.sc-native-hidden .fir-row,
.sc-native-hidden .row-elements,
.sc-native-hidden .text-box-container,
.sc-native-hidden .number-box,
.sc-native-hidden .noanswer-row,
.sc-native-hidden .error {
    display: none !important;
}

/* --- root container --- */
#sc-root {
    max-width: 640px;
    margin: 0 auto;
    padding: 24px 16px 120px;
    font-family: Inter, sans-serif;
    color: #333333;
}

/* --- PRISM logo top bar --- */
.sc-logo-bar {
    text-align: center;
    padding: 4px 0 36px;
}
.sc-logo-bar img {
    height: 40px;
    width: auto;
}

/* --- progress bar --- */
#sc-progress-wrap {
    position: sticky;
    top: 0; z-index: 200;
    background: #F5F2ED;
    padding: 8px 0 12px;
    display: none;
}
#sc-progress-track {
    height: 5px;
    background: #E0E0E0;
    border-radius: 3px;
}
#sc-progress-fill {
    height: 100%;
    width: 0%;
    background: #6B7F4E;
    border-radius: 3px;
    transition: width .45s cubic-bezier(.4,0,.2,1);
}
#sc-section-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 6px;
    font-family: Inter, sans-serif;
}
#sc-section-title {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: .06em;
    text-transform: uppercase;
    color: #888;
    text-decoration: underline;
    text-underline-offset: 2px;
}
#sc-progress-label {
    font-size: 9px;
    letter-spacing: .05em;
    color: #888888;
    font-weight: 600;
    text-transform: uppercase;
}

/* --- cards --- */
.sc-card {
    background: #FFFFFF;
    border: 1px solid #E0DCD6;
    border-radius: 10px;
    padding: 22px 24px 20px;
    margin-bottom: 28px;
    box-shadow: none;
    display: none;
}
.sc-card.sc-visible { display: block; }
.sc-card.sc-intro {
    display: block;
    background: #F8F5F0;
    padding: 28px 28px 26px;
}
.sc-q-label {
    font-family: Inter, sans-serif;
    font-size: 9px; font-weight: 600;
    letter-spacing: .08em; text-transform: uppercase;
    color: #888888; margin-bottom: 4px;
    text-align: right;
    display: none;
}
.sc-card .sc-q-text {
    font-family: Fraunces, Georgia, serif;
    font-size: 19px; font-weight: 700;
    color: #333333; line-height: 1.42;
    margin-bottom: 20px;
}

/* --- intro card --- */
.intro-body {
    font-family: Fraunces, Georgia, serif;
    font-size: 15px; color: #333333;
    text-align: center; line-height: 1.65;
    margin-bottom: 26px;
}
.intro-body em { font-style: italic; color: #6B7F4E; }

/* --- CTA / start button --- */
#sc-start-btn {
    display: block; margin: 0 auto;
    background: #6B7F4E; color: #fff;
    border: none; border-radius: 24px;
    padding: 14px 48px;
    font-size: 14px; font-weight: 700;
    letter-spacing: .1em; text-transform: uppercase;
    cursor: pointer; font-family: Inter, sans-serif;
    transition: background .2s, transform .15s;
}
#sc-start-btn:hover {
    background: #5A6B42;
    transform: translateY(-1px);
}
#sc-start-btn:disabled { background: #999; cursor: not-allowed; transform: none; }

/* --- QS1 yes/no/not-sure buttons --- */
.qs1-btn-group {
    display: grid;
    grid-template-columns: repeat(8, 1fr);
    align-items: flex-start;
    padding-top: 4px;
}
.qs1-btn.qs1-yes { grid-column: 2; justify-self: center; }
.qs1-btn.qs1-no  { grid-column: 4; justify-self: center; }
.qs1-btn.qs1-ns  { grid-column: 7; justify-self: center; }
.qs1-btn {
    display: flex; flex-direction: column;
    align-items: center; gap: 7px;
    cursor: pointer; user-select: none; outline: none;
}
.qs1-shape {
    display: flex; align-items: center; justify-content: center;
    font-weight: 800;
    transition: transform .18s, box-shadow .18s;
    box-shadow: 0 3px 10px rgba(0,0,0,.12);
}
.qs1-yes .qs1-shape {
    width: 78px; height: 78px; border-radius: 50%;
    background: #2E7D32; color: #fff; font-size: 30px;
}
.qs1-no .qs1-shape {
    width: 78px; height: 78px; border-radius: 50%;
    background: #C62828; color: #fff; font-size: 28px;
}
.qs1-ns .qs1-shape {
    width: 110px; height: 46px; border-radius: 10px;
    background: #D5D0C8; color: #555;
    font-size: 13px; font-weight: 600;
    box-shadow: 0 2px 8px rgba(0,0,0,.08);
}
.qs1-btn-label {
    font-size: 11px; font-weight: 700;
    letter-spacing: .1em; text-transform: uppercase; color: #888888;
}
.qs1-btn:hover .qs1-shape, .qs1-btn:focus .qs1-shape {
    transform: scale(1.06);
    box-shadow: 0 5px 16px rgba(0,0,0,.18);
}
.qs1-btn.sc-sel .qs1-shape {
    box-shadow: 0 0 0 4px rgba(107,127,78,.35), 0 3px 10px rgba(0,0,0,.15);
}
.qs1-btn.sc-sel .qs1-btn-label { color: #6B7F4E; }

/* --- ZIP code digits --- */
.zip-digits-row { display: flex; gap: 8px; margin-bottom: 14px; }
.zip-digit {
    width: 54px; height: 66px;
    border: 2px solid #D5D0C8; border-radius: 8px;
    font-size: 28px; font-weight: 700;
    text-align: center; color: #333333;
    background: #E8E4DE; outline: none;
    caret-color: #6B7F4E;
    font-family: Inter, sans-serif;
    transition: border-color .18s, box-shadow .18s, background .18s;
}
.zip-digit:focus {
    border-color: #6B7F4E; background: #fff;
    box-shadow: 0 0 0 3px rgba(107,127,78,.18);
}
.zip-digit.zip-filled { background: #fff; border-color: #6B7F4E; }

/* --- "Why we ask" callout --- */
.zip-why-row { margin-bottom: 10px; }
.zip-why-trigger {
    background: none; border: none; padding: 0; cursor: pointer;
    display: flex; align-items: center; gap: 7px;
    font-size: 12.5px; color: #888888; font-family: Inter, sans-serif;
}
.zip-why-icon {
    width: 20px; height: 20px; background: #D5D0C8; color: #555;
    border-radius: 50%;
    display: inline-flex; align-items: center; justify-content: center;
    font-size: 12px; font-weight: 800; flex-shrink: 0;
}
.zip-why-body {
    display: none; margin-top: 6px; padding: 10px 14px;
    background: #FFF9C4;
    border-left: 3px solid #6B7F4E; border-radius: 0 8px 8px 0;
    font-size: 12.5px; line-height: 1.55; color: #333333;
    font-family: Inter, sans-serif;
}

/* --- PNTA row --- */
.sc-pna-row {
    display: flex; align-items: center; gap: 9px;
    margin-top: 12px; cursor: pointer;
    font-size: 12.5px; color: #999999;
    font-family: Inter, sans-serif; user-select: none;
}
.sc-pna-chk { width: 18px; height: 18px; accent-color: #6B7F4E; cursor: pointer; }

/* --- Age digits --- */
.age-digits-row { display: flex; gap: 8px; margin-bottom: 14px; }
.age-digit {
    width: 58px; height: 70px;
    border: 2px solid #D5D0C8; border-radius: 8px;
    font-size: 30px; font-weight: 700;
    text-align: center; color: #333333;
    background: #E8E4DE; outline: none;
    font-family: Inter, sans-serif;
    transition: border-color .18s, box-shadow .18s, background .18s;
}
.age-digit:focus { border-color: #6B7F4E; background: #fff; box-shadow: 0 0 0 3px rgba(107,127,78,.18); }
.age-digit.age-filled { background: #fff; border-color: #6B7F4E; }
.sc-field-error {
  color: #C0392B; font-family: Inter, sans-serif; font-size: 13px;
  font-weight: 600; margin: -6px 0 12px; display: none;
}
.zip-digit.sc-input-err, .age-digit.sc-input-err {
  border-color: #C0392B !important; background: #FDF2F2 !important;
}
.age-instruction {
    font-size: 13px; color: #888888; margin: -10px 0 12px;
    font-family: Inter, sans-serif;
}

/* --- Gender: STYLE.HORIZONTAL.RADIO --- */
.gender-pills {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
}
.gender-pill:nth-child(1) { grid-column: 2; justify-self: center; }
.gender-pill:nth-child(2) { grid-column: 4; justify-self: center; }
.gender-pill:nth-child(3) { grid-column: 6; justify-self: center; }
.gender-pill {
    padding: 10px 24px;
    border: 2px solid #E0DCD6; border-radius: 24px;
    background: #fff; cursor: pointer;
    font-family: Inter, sans-serif; font-size: 13px; font-weight: 700;
    letter-spacing: .08em; text-transform: uppercase;
    color: #333333; text-align: center;
    transition: border-color .18s, background .18s;
    user-select: none;
}
.gender-pill:hover { border-color: #6B7F4E; }
.gender-pill.sc-sel { border-color: #6B7F4E; background: #6B7F4E; color: #fff; }

/* --- Vote24: CUSTOM.BALLOT --- */
.ballot-card-inner {
    border: 2px solid #333; border-radius: 3px;
    padding: 10px 12px 10px 22px;
    background: #fff; position: relative; overflow: hidden;
    font-family: 'Courier New', Courier, monospace;
}
.ballot-edge-bars {
    position: absolute; left: 0; top: 0; bottom: 0; width: 14px;
    display: flex; flex-direction: column;
    justify-content: space-around; padding: 6px 0;
    pointer-events: none;
}
.ballot-edge-bar { width: 10px; height: 6px; background: #333; border-radius: 1px; margin-left: 2px; }
.ballot-row {
    display: flex; align-items: center;
    padding: 8px 4px; border-bottom: 1px solid #ddd;
    cursor: pointer; position: relative;
    transition: background .12s;
}
.ballot-row:last-child { border-bottom: none; }
.ballot-row:hover { background: rgba(0,0,0,.03); }
.ballot-oval-wrap { width: 48px; flex-shrink: 0; display: flex; justify-content: center; }
.ballot-oval {
    width: 38px; height: 19px;
    border: 2.5px solid #333; border-radius: 50%;
    background: transparent;
    transition: background .15s;
}
.ballot-row.sc-sel .ballot-oval { background: #6B7F4E; }
.ballot-label {
    font-family: 'Courier New', Courier, monospace;
    font-size: 14px; color: #333333; font-weight: 600;
    letter-spacing: .02em; line-height: 1.3; padding-left: 4px; flex: 1;
}
.ballot-label .party-tag { font-weight: 400; margin-left: 4px; }
.ballot-row-muted .ballot-label { color: #999999; }
.ballot-bottom-row {
    display: flex; align-items: center;
    padding: 8px 4px; cursor: pointer;
    border-top: 1px solid #ddd;
    transition: background .12s;
}
.ballot-bottom-row:hover { background: rgba(0,0,0,.03); }
.ballot-bottom-row .ballot-label { color: #888888; font-size: 13px; }
.ballot-bottom-row.sc-sel .ballot-label { color: #333333; }
.ballot-bottom-row .ballot-oval-wrap { display: none; }

/* --- Party1: CUSTOM.PARTYID --- */
.party-main-btns {
    display: flex; gap: 14px;
    justify-content: flex-start; align-items: center;
    flex-wrap: wrap; margin-bottom: 6px;
}
.party-btn {
    display: flex; align-items: center; gap: 8px; cursor: pointer;
    padding: 10px 16px; border: 2px solid #E0DCD6;
    border-radius: 24px; background: #fff;
    transition: border-color .2s, background .2s;
    user-select: none;
}
.party-btn:hover { border-color: #6B7F4E; }
.party-btn.sc-sel { border-color: #6B7F4E; background: #F8F5F0; }
.party-btn .party-icon { width: 28px; height: 28px; border-radius: 50%; }
.party-btn > span { font-family: Inter, sans-serif; font-size: 14px; font-weight: 700; color: #333333; }
.party-ind-badge {
    display: inline-flex; align-items: center; justify-content: center;
    width: 28px; height: 28px; border-radius: 50%;
    background: #D5D0C8; color: #555;
    font-size: 9px; font-weight: 800; letter-spacing: .04em;
    flex-shrink: 0;
}

/* --- Party2: strong/not-so-strong (radio circles) --- */
.party-sub {
    display: none; margin-top: 18px;
    padding-top: 16px; border-top: 1px solid #E0DCD6;
}
.party-sub .sc-q-text { font-size: 16px; margin-bottom: 14px; }
.party-sub-radios {
    display: flex; gap: 28px; align-items: center;
    flex-wrap: wrap; margin-bottom: 14px;
}
.party-sub-radio {
    display: flex; align-items: center; gap: 8px;
    cursor: pointer; user-select: none;
}
.party-sub-circle {
    width: 28px; height: 28px;
    border: 2.5px solid #D5D0C8; border-radius: 50%;
    background: transparent;
    transition: background .15s, border-color .15s;
    flex-shrink: 0;
}
.party-sub-radio.sc-sel .party-sub-circle { background: #6B7F4E; border-color: #6B7F4E; }
.party-sub-radio > span {
    font-family: Inter, sans-serif; font-size: 15px; font-weight: 500; color: #333333;
}
.party-sub-icons {
    display: flex; gap: 10px; align-items: center; margin-top: 4px;
}
.party-sub-icon-pill {
    display: flex; align-items: center; gap: 6px;
    padding: 6px 14px; border: 2px solid #E0DCD6;
    border-radius: 20px; background: #fff;
}
.party-sub-icon-pill.sc-active { border-color: #6B7F4E; background: #F8F5F0; }
.party-sub-icon-pill.sc-dim { opacity: .4; }
.party-sub-icon-pill .party-icon { width: 22px; height: 22px; }
.party-sub-icon-pill > span { font-family: Inter, sans-serif; font-size: 13px; font-weight: 600; color: #333333; }

/* --- Party3: lean pills with icons --- */
.party-lean-btns {
    display: flex; gap: 10px; flex-wrap: wrap; align-items: center;
}
.party-lean-btn {
    display: flex; align-items: center; gap: 8px;
    padding: 10px 16px; border: 2px solid #E0DCD6; border-radius: 24px;
    background: #fff; cursor: pointer;
    transition: border-color .18s, background .18s;
    user-select: none;
}
.party-lean-btn:hover { border-color: #6B7F4E; }
.party-lean-btn.sc-sel { border-color: #6B7F4E; background: #F8F5F0; }
.party-lean-btn .party-icon { width: 24px; height: 24px; flex-shrink: 0; }
.party-lean-btn > span { font-size: 14px; font-weight: 600; color: #333333; font-family: Inter, sans-serif; }
.party-lean-no-badge {
    display: inline-flex; align-items: center; justify-content: center;
    width: 24px; height: 24px; border-radius: 50%;
    background: #D5D0C8; color: #555;
    font-size: 9px; font-weight: 800; flex-shrink: 0;
}

/* --- Footer --- */
#sc-footer {
    position: fixed; bottom: 0; left: 0; right: 0; z-index: 100;
    background: #F5F2ED;
    border-top: 1px solid #E0DCD6;
    padding: 10px 20px;
    text-align: center;
    font-size: 10px; color: #AAAAAA; font-family: Inter, sans-serif; line-height: 1.4;
}
#sc-footer a { color: #6B7F4E; text-decoration: none; }

/* --- Custom CONTINUE button --- */
#sc-continue-btn {
    background: #6B7F4E;
    color: #fff; border: none;
    border-radius: 24px;
    padding: 14px 36px;
    font-family: Inter, sans-serif;
    font-size: 14px; font-weight: 700;
    letter-spacing: .08em; text-transform: uppercase;
    cursor: pointer;
    box-shadow: none;
    transition: background .2s, transform .15s;
}
#sc-continue-btn:hover {
    background: #5A6B42;
    transform: translateY(-1px);
}
</style>
]]></style>

<style name="respview.client.js" mode="after" wrap="ready"><![CDATA[
  /* Only run screener card JS if screener elements exist on this page */
  if(!document.getElementById('card-intro')){ return; }

  var defined = function(s,c){ return (c||document).querySelector(s); };
  var definedAll = function(s,c){ return [].slice.call((c||document).querySelectorAll(s)); };

  var CARDS = ['card-intro','card-qs1','card-qzip','card-qage','card-qgender','card-qvote24','card-party'];
  var cur = 0;
  var total = CARDS.length - 1;

  /* ---- find Decipher question element by label ---- */
  function findQ(q){
    return document.getElementById('q_'+q)
        || document.getElementById('question_'+q)
        || document.getElementById(q)
        || document.querySelector('[id$="_'+q+'"]')
        || document.querySelector('.question input[name*="'+q+'"]');
  }
  /* auto-detect: log all .question elements so we know the ID format */
  var allQ = document.querySelectorAll('.question');
  console.log('[init] All .question IDs:', [].map.call(allQ, function(e){return e.id;}));

  /* ---- hide native Decipher questions ---- */
  ['QS1','QZIP','QAGE','QGENDER','QVOTE24','QS5','QS5A','QS5B'].forEach(function(q){
    var el = findQ(q);
    if(el){
      el.classList.add('sc-native-hidden');
      var radios = el.querySelectorAll('input[type="radio"]');
      var texts  = el.querySelectorAll('input[type="text"],input[type="number"]');
      console.log('[init] '+q+' → id='+el.id+' — '+radios.length+' radios, '+texts.length+' text inputs');
      if(radios.length) console.log('[init]   values:', [].map.call(radios, function(r){return r.value+' name='+r.name;}));
    } else {
      console.warn('[init] '+q+' NOT FOUND in DOM');
    }
  });

  /* ---- write back to native Decipher inputs ---- */
  function setRadio(q, val){
    var box = findQ(q);
    if(!box){ console.warn('[setRadio] '+q+' NOT FOUND in DOM'); return; }
    var all = box.querySelectorAll('input[type="radio"]');
    console.log('[setRadio] '+q+' val='+val+' — found '+all.length+' radios:', [].map.call(all, function(r){return r.value;}));
    /* Position-based first (data-val="1" = 1st radio, etc.), then by value */
    var inp = null;
    var idx = parseInt(val,10);
    if(idx>0 && idx<=all.length) inp = all[idx-1];
    if(!inp) inp = box.querySelector('input[type="radio"][value="r'+val+'"]');
    if(!inp) inp = box.querySelector('input[type="radio"][value="'+val+'"]');
    if(inp){
      console.log('[setRadio] '+q+' MATCH → value='+inp.value+' name='+inp.name);
      inp.checked = true;
      try{ jQuery(inp).trigger('click').trigger('change'); }catch(e){}
      inp.click();
      inp.dispatchEvent(new Event('change', {bubbles:true}));
    } else {
      console.warn('[setRadio] '+q+': NO MATCH for val='+val);
    }
  }
  function setText(q, val){
    var box = findQ(q);
    if(!box){ console.warn('[setText] '+q+' NOT FOUND'); return; }
    var inp = box.querySelector('input[type="text"]');
    if(inp){
      console.log('[setText] '+q+' → "'+val+'"');
      inp.value = val;
      try{ jQuery(inp).trigger('change'); }catch(e){}
      inp.dispatchEvent(new Event('change', {bubbles:true}));
    } else {
      console.warn('[setText] '+q+': no text input found');
    }
  }
  function setNumber(q, val){
    var box = findQ(q);
    if(!box){ console.warn('[setNumber] '+q+' NOT FOUND'); return; }
    var inp = box.querySelector('input[type="text"],input[type="number"]');
    if(inp){
      console.log('[setNumber] '+q+' → "'+val+'"');
      inp.value = val;
      try{ jQuery(inp).trigger('change'); }catch(e){}
      inp.dispatchEvent(new Event('change', {bubbles:true}));
    } else {
      console.warn('[setNumber] '+q+': no input found');
    }
  }

  /* ---- card navigation ---- */
  var LABELS = {
    'card-qs1':'label-qs1','card-qzip':'label-qzip','card-qage':'label-qage',
    'card-qgender':'label-qgender','card-qvote24':'label-qvote24','card-party':'label-party'
  };
  function showCard(idx){
    CARDS.forEach(function(id,i){
      var el = document.getElementById(id);
      if(!el) return;
      var show = i===0 ? idx===0 : i<=idx;
      el.style.display = show ? 'block' : 'none';
      var lbl = LABELS[id] ? document.getElementById(LABELS[id]) : null;
      if(lbl) lbl.style.display = show ? 'block' : 'none';
    });
    updateProgress();
  }
  function advance(){
    if(cur < CARDS.length-1){ cur++; showCard(cur); }
    var last = document.getElementById(CARDS[cur]);
    if(last) last.scrollIntoView({behavior:'smooth',block:'start'});
  }
  function updateProgress(){
    var wrap = document.getElementById('sc-progress-wrap');
    var fill = document.getElementById('sc-progress-fill');
    var lbl  = document.getElementById('sc-progress-label');
    if(!wrap||!fill) return;
    if(cur>0){
      wrap.style.display='block';
      var pct = Math.round((cur/total)*100);
      fill.style.width = pct+'%';
      if(lbl) lbl.textContent = cur+' of '+total+' Questions';
    }
  }
  function enableNext(){
    jQuery('#sc-continue-wrap').show();
    var wrap = document.getElementById('sc-continue-wrap');
    if(wrap) wrap.scrollIntoView({behavior:'smooth',block:'center'});
  }

  showCard(0);

  /* INTRO */
  jQuery('#sc-start-btn').on('click', function(){ advance(); });

  /* QS1 - registered to vote */
  jQuery('#card-qs1').on('click', '.qs1-btn', function(){
    jQuery('#card-qs1 .qs1-btn').removeClass('sc-sel');
    jQuery(this).addClass('sc-sel');
    setRadio('QS1', jQuery(this).attr('data-val'));
    setTimeout(advance, 350);
  });

  /* ---- validation helpers ---- */
  function showFieldError(id, msg){
    var el = document.getElementById(id);
    if(el){ el.textContent = msg; el.style.display = 'block'; }
  }
  function clearFieldError(id){
    var el = document.getElementById(id);
    if(el){ el.textContent = ''; el.style.display = 'none'; }
  }
  function validateZip(code){
    if(!/^\d{5}$/.test(code)) return 'Please enter a valid 5-digit ZIP code.';
    var pfx = parseInt(code.substring(0,3),10);
    /* US ZIP prefixes run 005-999; gaps: 000-004, 213, 269, 343, 348, 353, 419,
       428-432, 517-519, 529-532, 552, 568, 578, 579, 589, 621, 632, 642, 643,
       659, 663, 731, 732, 742, 771, 817, 818, 819, 839, 848, 849, 854, 858,
       861, 862, 866-869, 876-884, 890-891, 896-899, 929, 952-959, 987 */
    var bad = [0,1,2,3,4,213,269,343,348,353,419,517,518,519,552,568,578,579,
      589,621,632,642,643,659,663,731,732,742,771,817,818,819,839,848,849,
      854,858,861,862,866,867,868,869,876,877,878,879,880,881,882,883,884,
      890,891,896,897,898,899,929,952,953,954,955,956,957,958,959,987];
    if(bad.indexOf(pfx)!==-1) return 'That doesn\u2019t appear to be a valid US ZIP code. Please double-check.';
    return '';
  }
  function validateAge(val){
    var n = parseInt(val,10);
    if(isNaN(n) || n<18) return 'You must be at least 18 to participate.';
    if(n>89) return 'Please enter an age between 18 and 89.';
    return '';
  }

  /* QZIP - zip code digits */
  var zipD = definedAll('#card-qzip .zip-digit');
  jQuery('#card-qzip').on('input', '.zip-digit', function(){
    this.value = this.value.replace(/\D/g,'');
    var idx = zipD.indexOf(this);
    if(this.value.length===1 && idx<zipD.length-1) zipD[idx+1].focus();
    jQuery(this).toggleClass('zip-filled', this.value.length>0);
    clearFieldError('zip-error');
    jQuery(zipD).removeClass('sc-input-err');
    jQuery('#zip-pna').prop('checked', false);
    var full = jQuery.map(zipD, function(d){return d.value;}).join('');
    if(full.length===5){
      var err = validateZip(full);
      if(err){
        showFieldError('zip-error', err);
        jQuery(zipD).addClass('sc-input-err');
      } else {
        setText('QZIP', full);
        setTimeout(advance, 400);
      }
    }
  });
  jQuery('#card-qzip').on('keydown', '.zip-digit', function(e){
    var idx = zipD.indexOf(this);
    if(e.key==='Backspace' && !this.value && idx>0) zipD[idx-1].focus();
  });
  jQuery('#zip-pna').on('change', function(){
    if(this.checked){
      jQuery(zipD).val('').prop('disabled',true).removeClass('zip-filled sc-input-err');
      clearFieldError('zip-error');
      setText('QZIP','');
      setTimeout(advance, 350);
    } else { jQuery(zipD).prop('disabled',false); }
  });
  jQuery('.zip-why-trigger').on('click', function(){
    jQuery('.zip-why-body').toggle();
  });

  /* QAGE - age digits */
  var ageD = definedAll('#card-qage .age-digit');
  jQuery('#card-qage').on('input', '.age-digit', function(){
    this.value = this.value.replace(/\D/g,'');
    var idx = ageD.indexOf(this);
    if(this.value.length===1 && idx<ageD.length-1) ageD[idx+1].focus();
    jQuery(this).toggleClass('age-filled', this.value.length>0);
    clearFieldError('age-error');
    jQuery(ageD).removeClass('sc-input-err');
    jQuery('#age-pna').prop('checked', false);
    var full = jQuery.map(ageD, function(d){return d.value;}).join('');
    if(full.length===2){
      var err = validateAge(full);
      if(err){
        showFieldError('age-error', err);
        jQuery(ageD).addClass('sc-input-err');
      } else {
        setNumber('QAGE', full);
        setTimeout(advance, 400);
      }
    }
  });
  jQuery('#card-qage').on('keydown', '.age-digit', function(e){
    var idx = ageD.indexOf(this);
    if(e.key==='Backspace' && !this.value && idx>0) ageD[idx-1].focus();
  });
  jQuery('#age-pna').on('change', function(){
    if(this.checked){
      jQuery(ageD).val('').prop('disabled',true).removeClass('age-filled sc-input-err');
      clearFieldError('age-error');
      setNumber('QAGE','');
      setTimeout(advance, 350);
    } else { jQuery(ageD).prop('disabled',false); }
  });

  /* QGENDER - horizontal pills */
  jQuery('#gender-pills').on('click', '.gender-pill', function(){
    jQuery('#gender-pills .gender-pill').removeClass('sc-sel');
    jQuery(this).addClass('sc-sel');
    jQuery('#gender-pna').prop('checked', false);
    setRadio('QGENDER', jQuery(this).attr('data-val'));
    setTimeout(advance, 350);
  });
  jQuery('#gender-pna').on('change', function(){
    if(this.checked){ setTimeout(advance, 350); }
  });

  /* QVOTE24 - custom ballot */
  jQuery('#ballot-vote24').on('click', '.ballot-row, .ballot-bottom-row', function(){
    jQuery('#ballot-vote24 .ballot-row, #ballot-vote24 .ballot-bottom-row').removeClass('sc-sel');
    jQuery(this).addClass('sc-sel');
    jQuery('#vote-pna').prop('checked', false);
    setRadio('QVOTE24', jQuery(this).attr('data-val'));
    setTimeout(advance, 350);
  });
  jQuery('#vote-pna').on('change', function(){
    if(this.checked){ setTimeout(advance, 350); }
  });

  /* QS5 / QS5A / QS5B - party identification */
  var partyChoice = '';
  jQuery('#card-party').on('click', '.party-main-btn', function(){
    jQuery('#card-party .party-main-btn').removeClass('sc-sel');
    jQuery(this).addClass('sc-sel');
    jQuery('#party-pna').prop('checked', false);
    var v = jQuery(this).attr('data-val');
    partyChoice = v;
    setRadio('QS5', v);
    jQuery('#party-pna-main').hide();
    if(v==='1'||v==='2'){
      jQuery('#party-sub-a').show();
      jQuery('#qs5a-pipe').text(v==='1' ? 'Republican' : 'Democrat');
      jQuery('#pill-gop').removeClass('sc-active sc-dim');
      jQuery('#pill-dem').removeClass('sc-active sc-dim');
      if(v==='1'){ jQuery('#pill-gop').addClass('sc-active'); jQuery('#pill-dem').addClass('sc-dim'); }
      else { jQuery('#pill-dem').addClass('sc-active'); jQuery('#pill-gop').addClass('sc-dim'); }
      jQuery('#party-sub-b').hide();
    } else {
      jQuery('#party-sub-a').hide();
      jQuery('#party-sub-b').show();
    }
  });
  jQuery('#party-sub-a').on('click', '.party-sub-radio', function(){
    jQuery('#party-sub-a .party-sub-radio').removeClass('sc-sel');
    jQuery(this).addClass('sc-sel');
    jQuery('#party2-pna').prop('checked', false);
    setRadio('QS5A', jQuery(this).attr('data-val'));
    enableNext();
  });
  jQuery('#party2-pna').on('change', function(){
    if(this.checked){ enableNext(); }
  });
  jQuery('#party-sub-b').on('click', '.party-lean-btn', function(){
    jQuery('#party-sub-b .party-lean-btn').removeClass('sc-sel');
    jQuery(this).addClass('sc-sel');
    jQuery('#party3-pna').prop('checked', false);
    setRadio('QS5B', jQuery(this).attr('data-val'));
    enableNext();
  });
  jQuery('#party3-pna').on('change', function(){
    if(this.checked){ enableNext(); }
  });
  jQuery('#party-pna').on('change', function(){
    if(this.checked){ enableNext(); }
  });

  /* CONTINUE button — validate then trigger native Decipher page submit */
  jQuery('#sc-continue-btn').on('click', function(){
    /* Re-validate ZIP and age before submitting */
    var zipVal = jQuery.map(zipD, function(d){return d.value;}).join('');
    var ageVal = jQuery.map(ageD, function(d){return d.value;}).join('');
    var blocked = false;

    if(zipVal.length && zipVal.length===5){
      var ze = validateZip(zipVal);
      if(ze){ showFieldError('zip-error',ze); jQuery(zipD).addClass('sc-input-err'); blocked=true; }
    }
    if(ageVal.length && ageVal.length===2){
      var ae = validateAge(ageVal);
      if(ae){ showFieldError('age-error',ae); jQuery(ageD).addClass('sc-input-err'); blocked=true; }
    }
    if(blocked){
      var errEl = document.querySelector('.sc-field-error[style*="block"]');
      if(errEl) errEl.scrollIntoView({behavior:'smooth',block:'center'});
      return;
    }

    console.log('[CONTINUE] clicked — dumping native input states:');
    ['QS1','QZIP','QAGE','QGENDER','QVOTE24','QS5','QS5A','QS5B'].forEach(function(q){
      var box = findQ(q);
      if(!box){ console.log('  '+q+': NOT IN DOM'); return; }
      var checked = box.querySelector('input[type="radio"]:checked');
      var text = box.querySelector('input[type="text"],input[type="number"]');
      if(checked) console.log('  '+q+': radio='+checked.value);
      else if(text) console.log('  '+q+': text="'+text.value+'"');
      else console.log('  '+q+': (no selection)');
    });
    /* Override !important by adding inline style with !important via setAttribute */
    var nxt = document.querySelector('.nextPage, .button[type="submit"], input[type="submit"]');
    if(!nxt) nxt = document.querySelector('.button, input.button');
    if(nxt){
      nxt.setAttribute('style','display:block !important;visibility:hidden;position:absolute;left:-9999px;');
      console.log('[CONTINUE] native button found, clicking:', nxt.tagName, nxt.className);
      nxt.click();
    } else {
      console.warn('[CONTINUE] NO native submit button found — trying form submit');
      var form = document.querySelector('form');
      if(form) form.submit();
    }
  });

  /* Surface Decipher server-side validation errors on hidden native questions */
  jQuery(function(){
    var nativeErrs = jQuery('.question.sc-native-hidden .error, .question.sc-native-hidden .cell.error');
    if(nativeErrs.length){
      nativeErrs.each(function(){
        var qEl = jQuery(this).closest('.question');
        var qId = qEl.attr('id') || '';
        if(qId.indexOf('ZIP')!==-1||qId.indexOf('zip')!==-1){
          showFieldError('zip-error', jQuery(this).text().trim() || 'Please enter a valid ZIP code.');
          jQuery(zipD).addClass('sc-input-err');
        }
        if(qId.indexOf('AGE')!==-1||qId.indexOf('age')!==-1){
          showFieldError('age-error', jQuery(this).text().trim() || 'Please enter a valid age.');
          jQuery(ageD).addClass('sc-input-err');
        }
      });
      var errEl = document.querySelector('.sc-field-error[style*="block"]');
      if(errEl) errEl.scrollIntoView({behavior:'smooth',block:'center'});
    }
  });
]]></style>

<html label="sc_page_ui"><![CDATA[
<style>
.nextPage, .button { display: none !important; }
/* pre-hide MaxDiff native elements to prevent flash on page transition */
.QMD1_maxdiff table, .QMD1_maxdiff .survey-questions-container,
.QMD1_maxdiff .col-legend-group, .QMD1_maxdiff .grouping-box,
.QMD3_maxdiff table, .QMD3_maxdiff .survey-questions-container,
.QMD3_maxdiff .col-legend-group, .QMD3_maxdiff .grouping-box { display: none !important; }
</style>

<div id="sc-footer">
  Your responses are held strictly confidential.
  See our <a href="#">Privacy Policy</a>
  and commitment to research ethics and integrity.
</div>

<div id="sc-root">

  <div class="sc-logo-bar">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM">
  </div>

  <div id="sc-progress-wrap">
    <div id="sc-section-header">
      <span id="sc-section-title">SECTION 1: BACKGROUND INFO</span>
      <span id="sc-progress-label"></span>
    </div>
    <div id="sc-progress-track"><div id="sc-progress-fill"></div></div>
  </div>

  <div class="sc-card sc-intro" id="card-intro">
    <p class="intro-body">
      Thanks for taking part. This survey helps us understand how people
      think about health&nbsp;care and public policy today. There are no
      right or wrong answers&thinsp;&mdash;&thinsp;we&rsquo;re interested
      in&nbsp;<em>your</em>&nbsp;point of view.<br><br>Let&rsquo;s begin.
    </p>
    <button id="sc-start-btn" type="button">READY, SET, GO!</button>
  </div>

  <div class="sc-q-label" id="label-qs1">1 of 6 Questions</div>
  <div class="sc-card" id="card-qs1">
    <div class="sc-q-text">Are you registered to vote?</div>

    <div class="qs1-btn-group">

      <div class="qs1-btn qs1-yes" data-val="1" tabindex="0" role="radio" aria-label="Yes">
        <div class="qs1-shape">&#10003;</div>
        <span class="qs1-btn-label">YES</span>
      </div>

      <div class="qs1-btn qs1-no" data-val="2" tabindex="0" role="radio" aria-label="No">
        <div class="qs1-shape">&#10005;</div>
        <span class="qs1-btn-label">NO</span>
      </div>

      <div class="qs1-btn qs1-ns" data-val="99" tabindex="0" role="radio" aria-label="Not Sure">
        <div class="qs1-shape">Not Sure</div>
      </div>

    </div>
  </div>

  <div class="sc-q-label" id="label-qzip">2 of 6 Questions</div>
  <div class="sc-card" id="card-qzip">
    <div class="sc-q-text">Please enter your 5-digit Zip Code</div>
    <p style="font-size:13px;color:#888888;margin:-12px 0 14px;font-family:Inter,sans-serif;">
      (the Zip Code of your primary residence where you are registered to vote)
    </p>

    <div class="zip-digits-row">
      <input class="zip-digit" id="sc-zip-1" type="tel" maxlength="1" inputmode="numeric"
             autocomplete="off" aria-label="ZIP digit 1">
      <input class="zip-digit" id="sc-zip-2" type="tel" maxlength="1" inputmode="numeric"
             autocomplete="off" aria-label="ZIP digit 2">
      <input class="zip-digit" id="sc-zip-3" type="tel" maxlength="1" inputmode="numeric"
             autocomplete="off" aria-label="ZIP digit 3">
      <input class="zip-digit" id="sc-zip-4" type="tel" maxlength="1" inputmode="numeric"
             autocomplete="off" aria-label="ZIP digit 4">
      <input class="zip-digit" id="sc-zip-5" type="tel" maxlength="1" inputmode="numeric"
             autocomplete="off" aria-label="ZIP digit 5">
    </div>

    <div class="sc-field-error" id="zip-error"></div>

    <div class="zip-why-row">
      <button class="zip-why-trigger" type="button">
        <span class="zip-why-icon">?</span>
        <span style="font-size:12px;letter-spacing:.05em;text-transform:uppercase;font-weight:600;">
          WHY DO WE ASK THIS?
        </span>
      </button>
      <div class="zip-why-body">
        Your ZIP code helps us ensure we&rsquo;re hearing from people across different parts
        of the country, building a representative sample.
        <strong>FUN FACT:</strong> ZIP codes tie you to your congressional district, so we can ask
        follow&#x2011;up questions about your community &#x2014; they never reveal your name or exact address.
      </div>
    </div>

    <label class="sc-pna-row">
      <input class="sc-pna-chk" id="zip-pna" type="checkbox">
      <span>Prefer not to answer</span>
    </label>
  </div>

  <div class="sc-q-label" id="label-qage">3 of 6 Questions</div>
  <div class="sc-card" id="card-qage">
    <div class="sc-q-text">Tell us how old you are.</div>
    <div class="age-instruction">Enter two digits &mdash; e.g., &ldquo;4&rdquo; then &ldquo;5&rdquo; for age 45</div>

    <div class="age-digits-row">
      <input class="age-digit" id="sc-age-1" type="tel" maxlength="1" inputmode="numeric"
             autocomplete="off" aria-label="Age tens digit">
      <input class="age-digit" id="sc-age-2" type="tel" maxlength="1" inputmode="numeric"
             autocomplete="off" aria-label="Age units digit">
    </div>

    <div class="sc-field-error" id="age-error"></div>

    <label class="sc-pna-row">
      <input class="sc-pna-chk" id="age-pna" type="checkbox">
      <span>Prefer not to respond</span>
    </label>
  </div>

  <div class="sc-q-label" id="label-qgender">4 of 6 Questions</div>
  <div class="sc-card" id="card-qgender">
    <div class="sc-q-text">What is your gender?</div>

    <div class="gender-pills" id="gender-pills">
      <div class="gender-pill" data-val="1">MALE</div>
      <div class="gender-pill" data-val="2">FEMALE</div>
      <div class="gender-pill" data-val="3">OTHER</div>
    </div>

    <label class="sc-pna-row">
      <input class="sc-pna-chk" id="gender-pna" type="checkbox">
      <span>Prefer not to respond</span>
    </label>
  </div>

  <div class="sc-q-label" id="label-qvote24">5 of 6 Questions</div>
  <div class="sc-card" id="card-qvote24">
    <div class="sc-q-text">Who did you vote for in the latest (2024) Presidential election?</div>

    <div class="ballot-card-inner" id="ballot-vote24">
      <div class="ballot-edge-bars">
        <div class="ballot-edge-bar"></div>
        <div class="ballot-edge-bar"></div>
        <div class="ballot-edge-bar"></div>
        <div class="ballot-edge-bar"></div>
        <div class="ballot-edge-bar"></div>
        <div class="ballot-edge-bar"></div>
        <div class="ballot-edge-bar"></div>
        <div class="ballot-edge-bar"></div>
      </div>

      <div class="ballot-row" data-val="1">
        <div class="ballot-oval-wrap"><span class="ballot-oval"></span></div>
        <span class="ballot-label">Donald J Trump <span class="party-tag">(R)</span></span>
      </div>
      <div class="ballot-row" data-val="2">
        <div class="ballot-oval-wrap"><span class="ballot-oval"></span></div>
        <span class="ballot-label">Kamala Harris <span class="party-tag">(D)</span></span>
      </div>
      <div class="ballot-row ballot-row-muted" data-val="3">
        <div class="ballot-oval-wrap"><span class="ballot-oval"></span></div>
        <span class="ballot-label">Another Candidate</span>
      </div>
      <div class="ballot-bottom-row" data-val="4">
        <span class="ballot-label">I didn&rsquo;t have the chance to vote in this election</span>
      </div>
    </div>

    <label class="sc-pna-row">
      <input class="sc-pna-chk" id="vote-pna" type="checkbox">
      <span>Prefer not to respond</span>
    </label>
  </div>

  <div class="sc-q-label" id="label-party">6 of 6 Questions</div>
  <div class="sc-card" id="card-party">
    <div class="sc-q-text">In politics today, do you consider yourself a&hellip;?</div>

    <div class="party-main-btns">
      <div class="party-btn party-main-btn" data-val="1"
           tabindex="0" role="radio" aria-label="Republican">
        <img class="party-icon" src="[rel GOP_Icon.svg]" alt="GOP">
        <span>Republican</span>
      </div>
      <div class="party-btn party-main-btn" data-val="2"
           tabindex="0" role="radio" aria-label="Democrat">
        <img class="party-icon" src="[rel DEM_icon.svg]" alt="DEM">
        <span>Democrat</span>
      </div>
      <div class="party-btn party-main-btn" data-val="3"
           tabindex="0" role="radio" aria-label="Independent / Other">
        <span class="party-ind-badge">IND</span>
        <span>Independent/<br>Other</span>
      </div>
    </div>

    <label class="sc-pna-row" id="party-pna-main">
      <input class="sc-pna-chk" id="party-pna" type="checkbox">
      <span>Prefer not to respond</span>
    </label>

    <div class="party-sub" id="party-sub-a">
      <div class="sc-q-text">
        Would you say you are a strong or not-so-strong <span id="qs5a-pipe">Republican</span>?
      </div>
      <div class="party-sub-radios">
        <div class="party-sub-radio" data-val="1">
          <span class="party-sub-circle"></span>
          <span>Strong</span>
        </div>
        <div class="party-sub-radio" data-val="2">
          <span class="party-sub-circle"></span>
          <span>Not-so-strong</span>
        </div>
      </div>
      <div class="party-sub-icons" id="party-sub-icons">
        <div class="party-sub-icon-pill" id="pill-gop">
          <img class="party-icon" src="[rel GOP_Icon.svg]" alt="GOP">
          <span>Republican</span>
        </div>
        <div class="party-sub-icon-pill" id="pill-dem">
          <img class="party-icon" src="[rel DEM_icon.svg]" alt="DEM">
          <span>Democrat</span>
        </div>
      </div>

      <label class="sc-pna-row">
        <input class="sc-pna-chk" id="party2-pna" type="checkbox">
        <span>Prefer not to respond</span>
      </label>
    </div>

    <div class="party-sub" id="party-sub-b">
      <div class="sc-q-text">Do you <strong>lean</strong> toward the&hellip;</div>
      <div class="party-lean-btns">
        <div class="party-lean-btn" data-val="1">
          <img class="party-icon" src="[rel GOP_Icon.svg]" alt="GOP">
          <span>Republicans</span>
        </div>
        <div class="party-lean-btn" data-val="2">
          <img class="party-icon" src="[rel DEM_icon.svg]" alt="DEM">
          <span>Democrats</span>
        </div>
        <div class="party-lean-btn" data-val="3">
          <span class="party-lean-no-badge">NO</span>
          <span>I do not lean toward either</span>
        </div>
      </div>

      <label class="sc-pna-row">
        <input class="sc-pna-chk" id="party3-pna" type="checkbox">
        <span>Prefer not to respond</span>
      </label>
    </div>

  </div>

  <div id="sc-continue-wrap" style="display:none; text-align:center; padding:24px 0 16px;">
    <button id="sc-continue-btn" type="button">CONTINUE &gt;</button>
  </div>

</div>

]]></html>



<radio label="QS1" optional="1">
  <title>Are you registered to vote?</title>
  <row label="r1"  value="1">Yes</row>
  <row label="r2"  value="2">No</row>
  <row label="r99" value="99">Not Sure</row>
</radio>

<text label="QZIP" optional="1" size="5" verify="zipcode">
  <title>Please enter your 5-digit ZIP Code</title>
</text>

<number label="QAGE" optional="1" size="3" verify="range(18,89)">
  <title>What is your age?</title>
</number>

<radio label="QGENDER" optional="1">
  <title>Which of the following best describes your gender?</title>
  <row label="r1" value="1">Male</row>
  <row label="r2" value="2">Female</row>
  <row label="r3" value="3">Other</row>
  <row label="r99" cond="0" value="99">Prefer not to answer</row>
</radio>

<radio label="QVOTE24" optional="1">
  <title>Who did you vote for in the latest (2024) Presidential election?</title>
  <row label="r1" value="1">Donald Trump (R)</row>
  <row label="r2" value="2">Kamala Harris (D)</row>
  <row label="r3" value="3">Someone else</row>
  <row label="r4" value="4">I did not have the chance to vote</row>
</radio>

<radio label="QS5" optional="1">
  <title>In politics today, do you consider yourself a&#x2026;?</title>
  <row label="r1" value="1">Republican</row>
  <row label="r2" value="2">Democrat</row>
  <row label="r3" value="3">Independent</row>
  <row label="r4" value="4">Other</row>
</radio>

<radio label="QS5A" optional="1">
  <title>Would you say you are a strong or not-so strong [pipe: QS5]?</title>
  <row label="r1" value="1">Strong</row>
  <row label="r2" value="2">Not so strong</row>
</radio>

<radio label="QS5B" optional="1">
  <title>Do you lean towards the Republican or Democratic Party?</title>
  <row label="r1" value="1">Lean Republican</row>
  <row label="r2" value="2">Lean Democrat</row>
  <row label="r3" value="3">Neither Party</row>
</radio>


<text label="vscreenout" optional="1" size="10" cond="0">
  <title>Screenout tracker</title>
</text>

<suspend/>



<exec cond="QS1.r2 or QS1.r99">
vscreenout.val = str('QS1')
setMarker('Screened-QS1')
</exec>
<term label="Term_QS1" cond="QS1.r2 or QS1.r99">QS1 termination</term>




<exec cond="QVOTE24.r4">
vscreenout.val = str('QVOTE24')
setMarker('Screened-QVOTE24')
</exec>
<term label="Term_VOTE24" cond="QVOTE24.r4">Did not vote</term>

<exec cond="QVOTE24.r3 and QS5B.r3">
vscreenout.val = str('QS5')
setMarker('Screened-QS5')
</exec>
<term label="Term_PARTYID" cond="QVOTE24.r3 and QS5B.r3">
  Voted for someone else and is Independent
</term>




<note>===================== MAXDIFF SETUP =====================</note>

<exec when="init">
def setupMaxDiffFile(fname, fileDelimiter="\t"):
    try:
        f = open("%s/%s" % (gv.survey.path, fname))
        mdObj = [ line.strip("\r\n").split(fileDelimiter) for line in f.readlines() ]
        d = dict( ("v%s_t%s" % (row[0], row[1]), row[2:]) for row in mdObj )
    except IOError:
        d = {}
    return d
def setupMaxDiffItemsI(d, vt, question):
    item_index = dict( (r.o.label.strip("item"), r.index) for r in question.rows )
    if vt not in d:
        raise Exception("MaxDiff design key '%s' not found. Available keys: %s" % (vt, ", ".join(sorted(d.keys())[:20])))
    items = d[vt]
    for r in question.rows:
        if r.o.label.strip("item") not in items:
            r.disabled = True
    question.rows.order = [ item_index[i] for i in items ]
    print "*****STAFF ONLY*****"
    print "Version_Task: %s" % vt
    for i in range(len(items)):
        print "Item %s: %s" % (i+1,items[i])
        p.maxdiff1.append(items[i])
</exec>
<suspend/>

<exec>
p.maxdiff1=[]
</exec>

<radio
  label="XPARTY"
  optional="1"
  where="execute">
  <title>Hidden: Derived Party ID (1-7)</title>
  <exec>
if QS5.r1:
    XPARTY.val = XPARTY.r1.index if QS5A.r1 else XPARTY.r2.index
elif QS5.r2:
    XPARTY.val = XPARTY.r7.index if QS5A.r1 else XPARTY.r6.index
elif QS5.r3 or QS5.r4:
    if QS5B.r1:   XPARTY.val = XPARTY.r3.index
    elif QS5B.r2: XPARTY.val = XPARTY.r5.index
    else:          XPARTY.val = XPARTY.r4.index
  </exec>
  <row label="r1">Strong Republican</row>
  <row label="r2">Not-so-strong Republican</row>
  <row label="r3">Lean Republican</row>
  <row label="r4">Independent/Other</row>
  <row label="r5">Lean Democrat</row>
  <row label="r6">Not-so-strong Democrat</row>
  <row label="r7">Strong Democrat</row>
</radio>

<radio
  label="TYPING_MODULE"
  optional="1"
  where="execute">
  <title>Hidden: Typing Module Assignment</title>
  <exec>
party = 4
if QS5.r1:
    party = 1 if QS5A.r1 else 2
elif QS5.r2:
    party = 7 if QS5A.r1 else 6
elif QS5.r3 or QS5.r4:
    if QS5B.r1:   party = 3
    elif QS5B.r2: party = 5
    else:          party = 4

if QVOTE24.r1:
    if party &lt;= 3:
        TYPING_MODULE.val = TYPING_MODULE.r1.index
    else:
        TYPING_MODULE.val = TYPING_MODULE.r3.index
elif QVOTE24.r2:
    if party &gt;= 5:
        TYPING_MODULE.val = TYPING_MODULE.r2.index
    else:
        TYPING_MODULE.val = TYPING_MODULE.r3.index
elif QVOTE24.r3:
    if QS5B.r1:
        TYPING_MODULE.val = TYPING_MODULE.r1.index
    elif QS5B.r2:
        TYPING_MODULE.val = TYPING_MODULE.r2.index
    else:
        TYPING_MODULE.val = TYPING_MODULE.r2.index
  </exec>
  <row label="r1">GOP</row>
  <row label="r2">DEM</row>
  <row label="r3">BOTH</row>
</radio>
<suspend/>
<quota label="quo12" cond="TYPING_MODULE.any" overquota="noqual" sheet="TYPING MODULE Quota"/>
<suspend/>


<note>===================== PAGE GROUP 3 -- DEM MaxDiff Block =====================</note>

<block label="b11" cond="TYPING_MODULE.r2 or TYPING_MODULE.r3">
  <note>MaxDiff Indices Template --Start DEM--</note>
  <exec when="init">
QMD1_md = setupMaxDiffFile("DemDesign.dat")
  </exec>
  <suspend/>
  <quota label="quo1" overquota="noqual" sheet="Dem Quota"/>
  <suspend/>
  <number
   label="QMD1_Version"
   size="3"
   verify="range(1,100)"
   where="execute,survey,report">
    <title>QMD1 - MaxDiff Version</title>
    <exec>
for x in p.markers:
    if "/Dem Quota/Dem_" in x:
        QMD1_Version.val = int(x.split("_")[-1])
        break
    </exec>
  </number>
  <suspend/>
  <exec>
p.startTime = timeSpent()
  </exec>

  <html label="QMD1_intro"><![CDATA[
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style type="text/css">
  body, .survey-body, .main-content { background: #F5F2ED !important; }
  .nextPage, .button {
    background: #333 !important; color: #fff !important; border: none !important;
    border-radius: 24px !important; padding: 14px 40px !important;
    font-family: Inter,sans-serif !important; font-size: 14px !important;
    font-weight: 700 !important; letter-spacing: .08em !important;
    text-transform: uppercase !important; cursor: pointer !important;
    max-width: 320px !important; margin: 0 auto !important;
    display: block !important;
  }
</style>
<div style="max-width:480px;margin:0 auto;padding:0 16px 16px;text-align:center;">
  <div style="padding:4px 0 36px;">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM" style="height:48px;width:auto;">
  </div>
  <div style="background:#fff;border:1px solid #E0DCD6;border-radius:12px;padding:28px 24px 24px;margin-bottom:24px;">
    <p style="font-family:Fraunces,Georgia,serif;font-size:15px;color:#333;line-height:1.55;margin:0 0 16px;">
      In this section we'll ask about a mix of political and social issues. Some questions may feel personal, and some statements you may agree with strongly and others not so much. Always just go with your first, honest reaction.
    </p>
    <p style="font-family:Fraunces,Georgia,serif;font-size:15px;color:#333;line-height:1.55;margin:0 0 16px;">
      You'll see a series of cards, each with four statements. On each card, choose the one that sounds <strong>most</strong> like you and the one that sounds <strong>least</strong> like you.
    </p>
    <p style="font-family:Fraunces,Georgia,serif;font-size:15px;color:#333;line-height:1.55;margin:0;">
      Some statements may show up again&#x2014;that's intentional. There are no right or wrong answers. Go with the statement that feels most true&#x2014;and most familiar&#x2014;to you.
    </p>
  </div>
</div>
]]></html>
  <suspend/>

  <note>DEM MaxDiff Loop: 10 items, 4 per task, 10 tasks</note>
  <loop label="QMD1_md_loop" vars="task">
    <title>QMD1 - DEM MaxDiff Loop</title>
    <block label="QMD1_md_block" randomize="1">
      <radio
     label="QDEM_[loopvar: task]"
     adim="cols"
     grouping="cols"
     shuffle="rows"
     ss:listDisplay="0"
     ss:questionClassNames="QMD1_maxdiff"
     unique="1">
        <title>Select the one statement that sounds MOST like your own point of view (on the left), and the statement that sounds LEAST like you (on the right)</title>
        <exec>
setupMaxDiffItemsI( QMD1_md, "v%d_t%d" % (QMD1_Version.val, [loopvar: task]), QDEM_[loopvar: task])
p.MDcount = str(QMD1_md_loop_expanded.order.index([loopvar: task]-1)+1)
        </exec>
        <col label="best"><strong>sounds MOST like me</strong></col>
        <col label="worst"><strong>sounds LEAST like me</strong></col>
        <row label="item1">Tech and data can improve care, but only if we stop bias, protect privacy, and provide benefits fairly.</row>
        <row label="item2">I trust mainstream experts and prefer step-by-step fixes&#x2014;strengthening the ACA, not ripping it up.</row>
        <row label="item3">Digital tools, AI, and start-ups can universalize care faster than big bureaucracies&#x2014;let&#x2019;s remove red tape.</row>
        <row label="item4">Corporate greed hurts ordinary people. Government must crack down on prices, ads, and junk food.</row>
        <row label="item5">Healthcare is a human right. We need Medicare-for-All or something very close to it.</row>
        <row label="item6">Unions fought for good coverage. Policy should protect employer benefits and bargaining power.</row>
        <row label="item7">Racism and inequality drive poor health. Equity must be built into every health decision and budget.</row>
        <row label="item8">Reproductive freedom&#x2014;including abortion access&#x2014;is non-negotiable and central to health policy.</row>
        <row label="item9">My faith calls me to fight for the poor and for broader access&#x2014;especially in under-served communities.</row>
        <row label="item10">Climate change is a public-health emergency. Clean energy and environmental justice are health policy.</row>
        <style mode="before" name="question.header"><![CDATA[
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,400&amp;family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet">
<style type="text/css">
    body, .survey-body, .main-content { background: #F5F2ED !important; }
    .QMD1_maxdiff .question-text { display: none !important; }
    .QMD1_maxdiff .comment { display: none !important; }
    .nextPage, .button { display: none !important; }
    .QMD1_maxdiff .survey-questions-container,
    .QMD1_maxdiff table,
    .QMD1_maxdiff .col-legend-group,
    .QMD1_maxdiff .grouping-box { display: none !important; }
    .QMD1_maxdiff .md-custom-ui { display: block !important; }

    .md-custom-ui { max-width: 640px; margin: 0 auto; }
    .md-header-row { display: flex; justify-content: space-between; padding: 0 8px 8px; }
    .md-header-row span { font-family: Inter,sans-serif; font-size: 10px; font-weight: 800;
        letter-spacing: .06em; text-transform: uppercase; }
    .md-header-most { color: #6B7F4E; }
    .md-header-least { color: #C62828; }
    .md-row { display: flex; align-items: center; padding: 12px 4px;
        border-bottom: 1px solid #EDE9E3; gap: 10px; }
    .md-row:last-child { border-bottom: none; }
    .md-row-text { flex: 1; font-family: Fraunces,Georgia,serif; font-size: 16px;
        color: #333; line-height: 1.5; text-align: center; padding: 0 8px; }
    .md-row:hover { background: rgba(0,0,0,.03); border-radius: 8px; }
    .md-row.md-has-best { background: rgba(107,127,78,.08); border-radius: 8px; }
    .md-row.md-has-worst { background: rgba(198,40,40,.06); border-radius: 8px; }

    .md-radio { -webkit-appearance: none; -moz-appearance: none; appearance: none;
        width: 44px; height: 44px; min-width: 44px; border-radius: 50%;
        border: 2px solid #C5C0B8;
        background: radial-gradient(circle at 35% 35%, #FAFAFA, #E8E4DE);
        box-shadow: 0 2px 6px rgba(0,0,0,.12), inset 0 1px 2px rgba(255,255,255,.6);
        cursor: pointer; transition: all .2s ease; outline: none; }
    .md-radio:hover { border-color: #999;
        box-shadow: 0 3px 10px rgba(0,0,0,.18), inset 0 1px 2px rgba(255,255,255,.6);
        transform: scale(1.08); }
    .md-radio.md-best-checked {
        background: radial-gradient(circle at 35% 35%, #8CAF6B, #5A7E3A);
        border-color: #4A6E2A;
        box-shadow: 0 3px 10px rgba(90,126,58,.4), inset 0 1px 3px rgba(255,255,255,.3); }
    .md-radio.md-worst-checked {
        background: radial-gradient(circle at 35% 35%, #E05555, #C62828);
        border-color: #A01E1E;
        box-shadow: 0 3px 10px rgba(198,40,40,.4), inset 0 1px 3px rgba(255,255,255,.3); }

    .md-progress-wrap { padding: 0 0 8px; }
    .md-section-header { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 6px; }
    .md-section-title { font-family: Inter,sans-serif; font-size: 9px; font-weight: 700;
        letter-spacing: .06em; text-transform: uppercase; color: #888;
        text-decoration: underline; text-underline-offset: 2px; }
    .md-progress-label { font-family: Inter,sans-serif; font-size: 9px; font-weight: 600;
        letter-spacing: .05em; text-transform: uppercase; color: #888; }
    .md-progress-track { height: 5px; background: #E0E0E0; border-radius: 3px; }
    .md-progress-fill { height: 100%; width: 0%; background: #6B7F4E; border-radius: 3px;
        transition: width .45s cubic-bezier(.4,0,.2,1); }
    .md-card-counter { font-family: Inter,sans-serif; font-size: 9px; font-weight: 600;
        letter-spacing: .08em; text-transform: uppercase; color: #888;
        margin-bottom: 4px; text-align: right; }
</style>
<div style="max-width:640px;margin:0 auto;padding:0 16px 16px;">
  <div style="text-align:center;padding:4px 0 36px;">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM" style="height:40px;width:auto;">
  </div>
  <div class="md-progress-wrap">
    <div class="md-section-header">
      <span class="md-section-title">SECTION 2: VALUES &amp; IDENTITY</span>
      <span class="md-progress-label">${p.MDcount} of ${len(QMD1_md_loop_expanded.order)} Questions</span>
    </div>
    <div class="md-progress-track"><div class="md-progress-fill" id="md-progress-fill"></div></div>
  </div>
  <div class="md-card-counter">Card ${p.MDcount} of ${len(QMD1_md_loop_expanded.order)}</div>
  <div style="background:#fff;border:1px solid #E0DCD6;border-radius:10px;padding:22px 24px 20px;margin-bottom:20px;">
    <div style="font-family:Fraunces,Georgia,serif;font-size:17px;font-weight:700;color:#333;line-height:1.42;margin-bottom:8px;">
      From the set of short statements expressing different points of view about politics, culture and health.
    </div>
    <div style="font-size:13px;color:#555;text-align:center;margin-bottom:14px;line-height:1.5;">
      <span style="color:#6B7F4E;font-weight:700;">&#x2B05;</span>
      Select one statement that sounds MOST like your own point of view, and
      the one statement that sounds LEAST like you
      <span style="color:#C62828;font-weight:700;">&#x27A1;</span>
    </div>
]]></style>
        <style name="question.top-legend"><![CDATA[
\@if ec.simpleList
    $(legends)
\@else
    <$(tag) class="maxdiff-header-legend row row-col-legends row-col-legends-top ${"mobile-top-row-legend " if mobileOnly else ""}${"GtTenColumns " if ec.colCount > 10 else ""}colCount-$(colCount)">
        ${"%s%s" % (legends.split("</th>")[0],"</th>")}
       $(left)
        ${"%s%s" % (legends.split("</th>")[1],"</th>")}
    </$(tag)>
    \@if not simple
  </tbody>
  <tbody>
    \@endif
\@endif
]]></style>
        <style name="question.row"><![CDATA[
\@if ec.simpleList
    $(elements)
\@else
    <$(tag) class="maxdiff-row row row-elements $(style) colCount-$(colCount)">
        ${"%s%s" % (elements.split("</td>")[0],"</td>")}
        $(left)
        ${"%s%s" % (elements.split("</td>")[1],"</td>")}
    </$(tag)>
\@endif
]]></style>
        <style mode="after" name="question.footer"><![CDATA[
  </div>
  <div style="text-align:center;padding:24px 0 16px;">
    <button type="button" id="md-continue-btn"
            style="background:#6B7F4E;color:#fff;border:none;border-radius:24px;padding:14px 36px;font-family:Inter,sans-serif;font-size:14px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;cursor:pointer;opacity:.4;pointer-events:none;">
      CONTINUE &gt;
    </button>
  </div>
  <div style="position:fixed;bottom:0;left:0;right:0;z-index:100;background:#F5F2ED;border-top:1px solid #E0DCD6;padding:10px 20px;text-align:center;font-size:10px;color:#AAA;font-family:Inter,sans-serif;line-height:1.4;">
    Your responses are held strictly confidential.
    See our <a href="#" style="color:#6B7F4E;text-decoration:none;">Privacy Policy</a>
    and commitment to research ethics and integrity.
  </div>
</div>
<script>
(function(){
  /* set progress bar width from Decipher template vars */
  var pFill = document.getElementById('md-progress-fill');
  if(pFill){
    var curCard = parseInt('${p.MDcount}',10) || 1;
    var totalCards = parseInt('${len(QMD1_md_loop_expanded.order)}',10) || 1;
    pFill.style.width = Math.round((curCard/totalCards)*100)+'%';
  }

  var q = document.querySelector('.QMD1_maxdiff');
  if(!q) return;

  var allRadios = [].slice.call(q.querySelectorAll('input[type="radio"]'));
  var groups = {};
  allRadios.forEach(function(r){ (groups[r.name] = groups[r.name]||[]).push(r); });
  var gnames = Object.keys(groups);
  console.log('[MD-DEM] radio groups found:', gnames);
  if(gnames.length < 2){ console.warn('[MD-DEM] need 2+ radio groups'); return; }

  var bestRadios = groups[gnames[0]];
  var worstRadios = groups[gnames[1]];
  console.log('[MD-DEM] best:', bestRadios.length, 'worst:', worstRadios.length);

  var items = [];
  bestRadios.forEach(function(r,i){
    var row = r.closest('tr, .row-element, .row, label, li');
    var text = '';
    if(row){
      var legend = row.querySelector('th.row-legend, td.row-legend, .row-legend-text, .row-legend');
      if(!legend) legend = row.querySelector('.text');
      text = legend ? legend.textContent.trim() : '';
      if(!text){
        var clone = row.cloneNode(true);
        clone.querySelectorAll('input,th.col-legend,td.col-legend').forEach(function(el){ el.remove(); });
        text = clone.textContent.trim();
      }
    }
    if(!text) text = 'Statement '+(i+1);
    /* strip any [label] prefix from Decipher row text */
    var bi = text.indexOf(']');
    if(bi >= 0 && bi < 30) text = text.substring(bi+1).trim();
    console.log('[MD-DEM] item'+i+' text:', JSON.stringify(text));
    items.push(text);
  });

  var wrap = document.createElement('div');
  wrap.className = 'md-custom-ui';
  wrap.innerHTML = '<div class="md-header-row">' +
    '<span class="md-header-most">MOST<br>LIKE ME</span>' +
    '<span class="md-header-least">LEAST<br>LIKE ME</span></div>';

  var bestSel = -1, worstSel = -1;

  function updateUI(){
    var rows = wrap.querySelectorAll('.md-row');
    var allBest = wrap.querySelectorAll('.md-radio-best');
    var allWorst = wrap.querySelectorAll('.md-radio-worst');
    for(var i=0;i<rows.length;i++){
      rows[i].classList.remove('md-has-best','md-has-worst');
      allBest[i].classList.remove('md-best-checked');
      allWorst[i].classList.remove('md-worst-checked');
      if(i===bestSel){ rows[i].classList.add('md-has-best'); allBest[i].classList.add('md-best-checked'); }
      if(i===worstSel){ rows[i].classList.add('md-has-worst'); allWorst[i].classList.add('md-worst-checked'); }
    }
    var btn = document.getElementById('md-continue-btn');
    if(btn){
      if(bestSel>=0 && worstSel>=0){ btn.style.opacity='1'; btn.style.pointerEvents='auto'; }
      else { btn.style.opacity='.4'; btn.style.pointerEvents='none'; }
    }
  }

  items.forEach(function(txt,i){
    var row = document.createElement('div');
    row.className = 'md-row';
    row.innerHTML = '<button type="button" class="md-radio md-radio-best" data-idx="'+i+'"></button>' +
      '<div class="md-row-text">'+txt+'</div>' +
      '<button type="button" class="md-radio md-radio-worst" data-idx="'+i+'"></button>';
    wrap.appendChild(row);
  });

  wrap.addEventListener('click', function(e){
    var btn = e.target.closest('.md-radio');
    if(!btn) return;
    var idx = parseInt(btn.getAttribute('data-idx'),10);
    if(btn.classList.contains('md-radio-best')){
      if(idx===worstSel) return;
      bestSel = (bestSel===idx) ? -1 : idx;
      if(bestSel>=0 && bestRadios[bestSel]){ bestRadios[bestSel].checked=true; bestRadios[bestSel].click(); }
    } else {
      if(idx===bestSel) return;
      worstSel = (worstSel===idx) ? -1 : idx;
      if(worstSel>=0 && worstRadios[worstSel]){ worstRadios[worstSel].checked=true; worstRadios[worstSel].click(); }
    }
    updateUI();
  });

  q.insertBefore(wrap, q.firstChild);

  var btn = document.getElementById('md-continue-btn');
  if(btn){
    btn.addEventListener('click', function(){
      if(bestSel<0||worstSel<0) return;
      var nxt = document.querySelector('.nextPage, .button[type="submit"], input[type="submit"]');
      if(!nxt) nxt = document.querySelector('.button, input.button');
      if(nxt){
        nxt.setAttribute('style','display:block !important;visibility:hidden;position:absolute;left:-9999px;');
        nxt.click();
      } else {
        var form = document.querySelector('form');
        if(form) form.submit();
      }
    });
  }
})();
</script>
]]></style>
      </radio>
      <suspend/>
    </block>
    <looprow label="1"><loopvar name="task">1</loopvar></looprow>
    <looprow label="2"><loopvar name="task">2</loopvar></looprow>
    <looprow label="3"><loopvar name="task">3</loopvar></looprow>
    <looprow label="4"><loopvar name="task">4</loopvar></looprow>
    <looprow label="5"><loopvar name="task">5</loopvar></looprow>
    <looprow label="6"><loopvar name="task">6</loopvar></looprow>
    <looprow label="7"><loopvar name="task">7</loopvar></looprow>
    <looprow label="8"><loopvar name="task">8</loopvar></looprow>
    <looprow label="9"><loopvar name="task">9</loopvar></looprow>
    <looprow label="10"><loopvar name="task">10</loopvar></looprow>
  </loop>

  <float
   label="QMD1_Timer"
   size="15"
   where="execute,survey,report">
    <title>QMD1 - DEM MaxDiff Timer (Minutes)</title>
    <exec>
QMD1_Timer.val = (timeSpent() - p.startTime) / 60.0
    </exec>
  </float>
  <suspend/>


  <note>===================== PAGE GROUP 4 -- DEM Vectors =====================</note>

  <radio
   label="VECTOR_JUSTICE"
   type="rating"
   values="order"
   averages="cols"
   ss:questionClassNames="vec_dem_q"
   optional="0">
    <title>Tell us how much you agree or disagree with these statements</title>
    <comment>Tap where you fall on each statement...</comment>
    <col label="c1" value="1">Strongly Disagree</col>
    <col label="c2" value="2">2</col>
    <col label="c3" value="3">3</col>
    <col label="c4" value="4">Neutral</col>
    <col label="c5" value="5">5</col>
    <col label="c6" value="6">6</col>
    <col label="c7" value="7">Strongly Agree</col>
    <row label="r1">The federal government should invest extra funds in communities of color that have suffered historic health injustices, even if it means higher taxes</row>
    <style mode="before" name="question.header"><![CDATA[
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,400&amp;family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet">
<style type="text/css">
    body, .survey-body, .main-content { background: #F5F2ED !important; }
    .vec_dem_q .question-text { display: none !important; }
    .vec_dem_q .comment { display: none !important; }
    .vec_dem_q .instruction-text { display: none !important; }
    .nextPage, .button { display: none !important; }
    .vec_dem_q .survey-questions-container,
    .vec_dem_q table,
    .vec_dem_q .col-legend-group,
    .vec_dem_q .grouping-box,
    .vec_dem_q .answers-list { visibility: hidden; height: 0; overflow: hidden; }
    .vec_dem_q .vec-custom-ui { visibility: visible; height: auto; overflow: visible; }

    .vec-wrap { max-width: 640px; margin: 0 auto; }
    .vec-card { background: #fff; border: 1px solid #E0DCD6; border-radius: 10px;
        padding: 22px 24px 20px; margin-bottom: 20px; }
    .vec-counter { font-size: 9px; font-weight: 600; letter-spacing: .08em;
        text-transform: uppercase; color: #888; margin-bottom: 4px; text-align: right; }
    .vec-title { font-family: Fraunces,Georgia,serif; font-size: 17px; font-weight: 700;
        color: #333; line-height: 1.42; margin-bottom: 4px; }
    .vec-subtitle { font-family: Inter,sans-serif; font-size: 13px; color: #999;
        font-style: italic; margin-bottom: 18px; }

    .vec-scale-labels { display: none; }
    .vec-dot-labels { display: flex; justify-content: space-between; padding: 2px 4px 0;
        font-family: Inter,sans-serif; font-size: 8px; font-weight: 600; text-transform: uppercase;
        letter-spacing: .04em; line-height: 1.2; }
    .vec-dot-labels span { text-align: center; width: 36px; min-width: 36px; }
    .vec-dot-labels span:first-child { color: #C62828; }
    .vec-dot-labels span:nth-child(2) { color: #D68080; }
    .vec-dot-labels span:nth-child(3) { color: #E0A8A8; }
    .vec-dot-labels span:nth-child(4) { color: #999; }
    .vec-dot-labels span:nth-child(5) { color: #A8CCA8; }
    .vec-dot-labels span:nth-child(6) { color: #7DB87D; }
    .vec-dot-labels span:last-child { color: #4A6E2A; }

    .vec-item { padding: 14px 0; border-bottom: 1px solid #EDE9E3; }
    .vec-item:last-child { border-bottom: none; }
    .vec-item-text { font-family: Fraunces,Georgia,serif; font-size: 14px;
        color: #333; line-height: 1.45; margin-bottom: 10px; }
    .vec-dots { display: flex; justify-content: space-between; align-items: center; padding: 0 4px; }

    .vec-dot { -webkit-appearance: none; -moz-appearance: none; appearance: none;
        width: 36px; height: 36px; min-width: 36px; border-radius: 50%;
        border: 2px solid #C5C0B8;
        background: radial-gradient(circle at 35% 35%, #FAFAFA, #E8E4DE);
        box-shadow: 0 2px 6px rgba(0,0,0,.12), inset 0 1px 2px rgba(255,255,255,.6);
        cursor: pointer; transition: all .2s ease; outline: none; }
    /* hover colors by position: red pole → gray → green pole */
    .vec-dot[data-di="0"]:hover { border-color: #A01E1E;
        background: radial-gradient(circle at 35% 35%, #E05555, #C62828);
        box-shadow: 0 3px 10px rgba(198,40,40,.3), inset 0 1px 2px rgba(255,255,255,.4);
        transform: scale(1.08); }
    .vec-dot[data-di="1"]:hover { border-color: #D68080;
        background: radial-gradient(circle at 35% 35%, #F5BFBF, #E89999);
        box-shadow: 0 3px 10px rgba(211,47,47,.2), inset 0 1px 2px rgba(255,255,255,.4);
        transform: scale(1.08); }
    .vec-dot[data-di="2"]:hover { border-color: #E0A8A8;
        background: radial-gradient(circle at 35% 35%, #FADCDC, #F2C4C4);
        box-shadow: 0 3px 10px rgba(239,83,80,.15), inset 0 1px 2px rgba(255,255,255,.4);
        transform: scale(1.08); }
    .vec-dot[data-di="3"]:hover { border-color: #888;
        background: radial-gradient(circle at 35% 35%, #BDBDBD, #9E9E9E);
        box-shadow: 0 3px 10px rgba(0,0,0,.18), inset 0 1px 2px rgba(255,255,255,.4);
        transform: scale(1.08); }
    .vec-dot[data-di="4"]:hover { border-color: #A8CCA8;
        background: radial-gradient(circle at 35% 35%, #DCEDC8, #C5E1A5);
        box-shadow: 0 3px 10px rgba(129,199,132,.15), inset 0 1px 2px rgba(255,255,255,.4);
        transform: scale(1.08); }
    .vec-dot[data-di="5"]:hover { border-color: #7DB87D;
        background: radial-gradient(circle at 35% 35%, #B5D8A0, #93C47D);
        box-shadow: 0 3px 10px rgba(102,187,106,.2), inset 0 1px 2px rgba(255,255,255,.4);
        transform: scale(1.08); }
    .vec-dot[data-di="6"]:hover { border-color: #4A6E2A;
        background: radial-gradient(circle at 35% 35%, #8CAF6B, #5A7E3A);
        box-shadow: 0 3px 10px rgba(90,126,58,.3), inset 0 1px 2px rgba(255,255,255,.4);
        transform: scale(1.08); }
    /* checked states by position */
    .vec-dot.vec-c1 {
        background: radial-gradient(circle at 35% 35%, #E05555, #C62828);
        border-color: #A01E1E;
        box-shadow: 0 3px 10px rgba(198,40,40,.4), inset 0 1px 3px rgba(255,255,255,.3); }
    .vec-dot.vec-c2 {
        background: radial-gradient(circle at 35% 35%, #F5BFBF, #E89999);
        border-color: #D68080;
        box-shadow: 0 3px 10px rgba(211,47,47,.25), inset 0 1px 3px rgba(255,255,255,.3); }
    .vec-dot.vec-c3 {
        background: radial-gradient(circle at 35% 35%, #FADCDC, #F2C4C4);
        border-color: #E0A8A8;
        box-shadow: 0 3px 10px rgba(239,83,80,.2), inset 0 1px 3px rgba(255,255,255,.3); }
    .vec-dot.vec-c4 {
        background: radial-gradient(circle at 35% 35%, #BDBDBD, #9E9E9E);
        border-color: #888;
        box-shadow: 0 3px 10px rgba(0,0,0,.2), inset 0 1px 3px rgba(255,255,255,.3); }
    .vec-dot.vec-c5 {
        background: radial-gradient(circle at 35% 35%, #DCEDC8, #C5E1A5);
        border-color: #A8CCA8;
        box-shadow: 0 3px 10px rgba(129,199,132,.25), inset 0 1px 3px rgba(255,255,255,.3); }
    .vec-dot.vec-c6 {
        background: radial-gradient(circle at 35% 35%, #B5D8A0, #93C47D);
        border-color: #7DB87D;
        box-shadow: 0 3px 10px rgba(102,187,106,.3), inset 0 1px 3px rgba(255,255,255,.3); }
    .vec-dot.vec-c7 {
        background: radial-gradient(circle at 35% 35%, #8CAF6B, #5A7E3A);
        border-color: #4A6E2A;
        box-shadow: 0 3px 10px rgba(90,126,58,.4), inset 0 1px 3px rgba(255,255,255,.3); }

    .vec-prefer-wrap { padding: 12px 0 0; display: flex; align-items: center; gap: 8px;
        font-family: Inter,sans-serif; font-size: 13px; color: #666; }
    .vec-prefer-wrap input { width: 16px; height: 16px; cursor: pointer; }
</style>
<div class="vec-wrap" style="padding:0 16px 16px;">
  <div style="text-align:center;padding:4px 0 36px;">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM" style="height:40px;width:auto;">
  </div>
  <div class="vec-counter">1 of 1 Questions</div>
  <div class="vec-card">
    <div class="vec-title">Tell us how much you agree or disagree with these statements</div>
    <div class="vec-subtitle">Tap where you fall on each statement...</div>
    <div class="vec-scale-labels">
      <span>Strongly<br>Disagree</span>
      <span>Neutral</span>
      <span>Strongly<br>Agree</span>
    </div>
    <div class="vec-prefer-wrap">
      <input type="checkbox" id="vec-prefer-skip">
      <label for="vec-prefer-skip">Prefer not to respond</label>
    </div>
]]></style>
  </radio>

  <radio
   label="VECTOR_INDUSTRY"
   type="rating"
   values="order"
   averages="cols"
   ss:questionClassNames="vec_dem_q"
   optional="0">
    <title>Tell us how much you agree or disagree with these statements</title>
    <col label="c1" value="1">Strongly Disagree</col>
    <col label="c2" value="2">2</col>
    <col label="c3" value="3">3</col>
    <col label="c4" value="4">Neutral</col>
    <col label="c5" value="5">5</col>
    <col label="c6" value="6">6</col>
    <col label="c7" value="7">Strongly Agree</col>
    <row label="r1">Big corporations have too much influence over public health policies</row>
    <style mode="after" name="question.footer"><![CDATA[
  </div>
  <div style="text-align:center;padding:24px 0 16px;">
    <button type="button" id="vec-continue-btn"
            style="background:#6B7F4E;color:#fff;border:none;border-radius:24px;padding:14px 36px;font-family:Inter,sans-serif;font-size:14px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;cursor:pointer;opacity:.4;pointer-events:none;">
      CONTINUE &gt;
    </button>
  </div>
  <div style="position:fixed;bottom:0;left:0;right:0;z-index:100;background:#F5F2ED;border-top:1px solid #E0DCD6;padding:10px 20px;text-align:center;font-size:10px;color:#AAA;font-family:Inter,sans-serif;line-height:1.4;">
    Your responses are held strictly confidential.
    See our <a href="#" style="color:#6B7F4E;text-decoration:none;">Privacy Policy</a>
    and commitment to research ethics and integrity.
  </div>
</div>
<script>
(function(){
  /* --- find native radios for both questions --- */
  var boxes = document.querySelectorAll('.vec_dem_q');
  if(boxes.length < 2) return;

  var questions = [];
  boxes.forEach(function(box){
    var radios = [].slice.call(box.querySelectorAll('input[type="radio"]'));
    var row = box.querySelector('th.row-legend, td.row-legend, .row-legend-text, .row-legend, .text');
    var text = row ? row.textContent.trim() : '';
    if(!text){
      /* fallback: get from row element text */
      var rowEl = box.querySelector('.row-elements, .row-element, tr.row');
      if(rowEl){
        var clone = rowEl.cloneNode(true);
        clone.querySelectorAll('input,th.col-legend,td.col-legend').forEach(function(el){ el.remove(); });
        text = clone.textContent.trim();
      }
    }
    /* strip any [label] prefix */
    var bi = text.indexOf(']');
    if(bi >= 0 && bi < 30) text = text.substring(bi+1).trim();
    questions.push({ radios: radios, text: text || 'Statement' });
  });

  /* --- build custom UI --- */
  var card = document.querySelector('.vec-card');
  if(!card) return;
  var preferWrap = card.querySelector('.vec-prefer-wrap');

  var selections = [-1, -1]; /* index of selected dot per question */

  function updateContinue(){
    var btn = document.getElementById('vec-continue-btn');
    var skip = document.getElementById('vec-prefer-skip');
    if(!btn) return;
    var allDone = (selections[0] >= 0 && selections[1] >= 0);
    var skipped = skip && skip.checked;
    if(allDone || skipped){
      btn.style.opacity = '1'; btn.style.pointerEvents = 'auto';
    } else {
      btn.style.opacity = '.4'; btn.style.pointerEvents = 'none';
    }
  }

  questions.forEach(function(qObj, qi){
    var item = document.createElement('div');
    item.className = 'vec-item';
    item.innerHTML = '<div class="vec-item-text">' + qObj.text + '</div><div class="vec-dots"></div>' +
      '<div class="vec-dot-labels"><span>Strongly Disagree</span><span></span><span></span><span>Neutral</span><span></span><span></span><span>Strongly Agree</span></div>';
    var dotsWrap = item.querySelector('.vec-dots');

    for(var d = 0; d < 7; d++){
      var dot = document.createElement('button');
      dot.type = 'button';
      dot.className = 'vec-dot';
      dot.setAttribute('data-qi', qi);
      dot.setAttribute('data-di', d);
      dotsWrap.appendChild(dot);
    }

    dotsWrap.addEventListener('click', function(e){
      var dot = e.target.closest('.vec-dot');
      if(!dot) return;
      var di = parseInt(dot.getAttribute('data-di'), 10);
      /* toggle off if same dot clicked */
      if(selections[qi] === di){
        selections[qi] = -1;
      } else {
        selections[qi] = di;
        /* sync to native radio */
        if(qObj.radios[di]){
          qObj.radios[di].checked = true;
          qObj.radios[di].click();
        }
      }
      /* update dot visuals — color by position */
      var allDots = dotsWrap.querySelectorAll('.vec-dot');
      var cnames = ['vec-c1','vec-c2','vec-c3','vec-c4','vec-c5','vec-c6','vec-c7'];
      allDots.forEach(function(dd, idx){
        cnames.forEach(function(cn){ dd.classList.remove(cn); });
        if(idx === selections[qi]) dd.classList.add(cnames[idx]);
      });
      /* uncheck prefer-not-to-respond when a dot is selected */
      var skip = document.getElementById('vec-prefer-skip');
      if(skip) skip.checked = false;
      updateContinue();
    });

    /* insert items BEFORE the prefer-not checkbox */
    if(preferWrap){
      card.insertBefore(item, preferWrap);
    } else {
      card.appendChild(item);
    }
  });

  /* --- prefer-not-to-respond checkbox --- */
  var skip = document.getElementById('vec-prefer-skip');
  if(skip){
    skip.addEventListener('change', function(){
      if(skip.checked){
        /* clear selections visually */
        var cnames = ['vec-c1','vec-c2','vec-c3','vec-c4','vec-c5','vec-c6','vec-c7'];
        card.querySelectorAll('.vec-dot').forEach(function(d){ cnames.forEach(function(cn){ d.classList.remove(cn); }); });
        selections = [-1, -1];
        /* set native radios to neutral (c4) as default for skippers */
        questions.forEach(function(qObj){
          if(qObj.radios[3]){ qObj.radios[3].checked = true; qObj.radios[3].click(); }
        });
      }
      updateContinue();
    });
  }

  /* --- continue button --- */
  var btn = document.getElementById('vec-continue-btn');
  if(btn){
    btn.addEventListener('click', function(){
      var nxt = document.querySelector('.nextPage');
      if(!nxt) nxt = document.querySelector('input.button');
      if(nxt){
        nxt.setAttribute('style','display:block !important;visibility:hidden;position:absolute;left:-9999px;');
        nxt.click();
      } else {
        var form = document.querySelector('form');
        if(form) form.submit();
      }
    });
  }
})();
</script>
]]></style>
  </radio>
  <suspend/>

<note>===================== PAGE GROUP 5 -- INFLUENCE360_SCREEN =====================</note>

<checkbox
  label="INF1"
  atleast="1"
  optional="0"
  ss:questionClassNames="inf_hidden">
  <title>How have you made your voice heard?</title>
  <comment>Select all that apply. At the end, you'll see your Influence Compass.</comment>
  <row label="r1">I've worked inside government, held office, or staffed those who do</row>
  <row label="r2">I've helped decide who runs for office or who gets hired in political or government roles</row>
  <row label="r3">I advise organizations, sit on boards, or have testified on policy matters</row>
  <row label="r4">I've written, spoken publicly, or published on issues</row>
  <row label="r5">I've organized people, worked campaigns, or rallied others to action</row>
  <row label="r6">I vote, follow current events, and stay involved in my community</row>
  <row label="r99" exclusive="1">None of these</row>

  <style mode="before" name="question.header"><![CDATA[
<style>
/* --- hide native Decipher question elements on influence pages --- */
.inf_hidden {
    border: none !important;
    box-shadow: none !important;
    background: transparent !important;
    padding: 0 !important;
    margin: 0 !important;
    min-height: 0 !important;
    overflow: visible !important;
}
.inf_hidden .question-text,
.inf_hidden .comment,
.inf_hidden .instruction-text,
.inf_hidden .answers-list,
.inf_hidden .fir-row,
.inf_hidden .row-elements,
.inf_hidden .noanswer-row,
.inf_hidden .error,
.inf_hidden .mobile-grid-wrapper,
.inf_hidden .grid-table-mode,
.inf_hidden .cell.noAnswer {
    display: none !important;
}
/* hide native Next/submit button */
.nextPage, .button[type="submit"], input[type="submit"] {
    display: none !important;
}

/* --- INF root container --- */
#inf-root {
    max-width: 640px;
    margin: 0 auto;
    padding: 24px 16px 120px;
    font-family: Inter, sans-serif;
    color: #333333;
}

/* --- PRISM logo --- */
.inf-logo-bar {
    text-align: center;
    padding: 4px 0 36px;
}
.inf-logo-bar img {
    height: 40px;
    width: auto;
}

/* --- Section header + progress --- */
.inf-progress-wrap {
    position: sticky;
    top: 0; z-index: 200;
    background: #F5F2ED;
    padding: 8px 0 12px;
}
.inf-section-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 6px;
    font-family: Inter, sans-serif;
}
.inf-section-title {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: .06em;
    text-transform: uppercase;
    color: #888;
    text-decoration: underline;
    text-underline-offset: 2px;
}
.inf-progress-label {
    font-size: 9px;
    letter-spacing: .05em;
    color: #888888;
    font-weight: 600;
    text-transform: uppercase;
}
.inf-progress-track {
    height: 5px;
    background: #E0E0E0;
    border-radius: 3px;
}
.inf-progress-fill {
    height: 100%;
    width: 33%;
    background: #6B7F4E;
    border-radius: 3px;
    transition: width .45s cubic-bezier(.4,0,.2,1);
}

/* --- Intro card --- */
.inf-intro-card {
    background: #F8F5F0;
    border: 1px solid #E0DCD6;
    border-radius: 10px;
    padding: 28px 28px 26px;
    margin-bottom: 28px;
}
.inf-intro-body {
    font-family: Fraunces, Georgia, serif;
    font-size: 15px;
    color: #333333;
    text-align: center;
    line-height: 1.65;
}

/* --- Question card --- */
.inf-card {
    background: #FFFFFF;
    border: 1px solid #E0DCD6;
    border-radius: 10px;
    padding: 22px 24px 20px;
    margin-bottom: 28px;
}
.inf-q-label {
    font-family: Inter, sans-serif;
    font-size: 9px; font-weight: 600;
    letter-spacing: .08em; text-transform: uppercase;
    color: #888888; margin-bottom: 4px;
    text-align: right;
}
.inf-q-title {
    font-family: Fraunces, Georgia, serif;
    font-size: 19px; font-weight: 700;
    color: #333333; line-height: 1.42;
    margin-bottom: 6px;
}
.inf-q-subtitle {
    font-family: Inter, sans-serif;
    font-size: 12px; color: #888888;
    margin-bottom: 18px;
}

/* --- Checklist checkbox rows --- */
.inf-check-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 12px 14px;
    border-bottom: 1px solid #F0EDE8;
    cursor: pointer;
    user-select: none;
    transition: background .15s;
}
.inf-check-row:last-of-type {
    border-bottom: none;
}
.inf-check-row:hover {
    background: #FAFAF8;
}
.inf-check-box {
    flex-shrink: 0;
    width: 22px; height: 22px;
    border: 2px solid #CCCCCC;
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: border-color .15s, background .15s;
    margin-top: 1px;
}
.inf-check-row.inf-checked .inf-check-box {
    border-color: #6B7F4E;
    background: #6B7F4E;
}
.inf-check-box svg {
    display: none;
    width: 14px; height: 14px;
}
.inf-check-row.inf-checked .inf-check-box svg {
    display: block;
}
.inf-check-label {
    font-family: Inter, sans-serif;
    font-size: 14px;
    color: #333333;
    line-height: 1.45;
    flex: 1;
}

/* --- Merged checklist separator + subhead --- */
.inf-merged-separator {
    height: 1px;
    background: #E0DCD6;
    margin: 16px 0 12px;
}
.inf-merged-subhead {
    font-family: Fraunces, Georgia, serif;
    font-size: 15px;
    font-weight: 600;
    color: #333;
    line-height: 1.4;
    margin-bottom: 12px;
    padding: 0 14px;
}

/* --- "None of these" button --- */
.inf-none-btn {
    display: inline-block;
    padding: 8px 18px;
    border: 1px solid #333;
    border-radius: 6px;
    background: #fff;
    font-family: Inter, sans-serif;
    font-size: 13px;
    font-weight: 600;
    color: #333;
    cursor: pointer;
    margin-top: 14px;
    transition: background .15s, border-color .15s;
}
.inf-none-btn:hover {
    background: #F5F2ED;
}
.inf-none-btn.inf-sel {
    background: #333;
    color: #fff;
}


/* --- Continue button --- */
.inf-continue-wrap {
    text-align: center;
    padding: 24px 0 12px;
}
.inf-continue-btn {
    background: #6B7F4E;
    color: #fff; border: none;
    border-radius: 24px;
    padding: 14px 36px;
    font-family: Inter, sans-serif;
    font-size: 14px; font-weight: 700;
    letter-spacing: .08em; text-transform: uppercase;
    cursor: pointer;
    transition: background .2s, transform .15s;
}
.inf-continue-btn:hover {
    background: #5A6B42;
    transform: translateY(-1px);
}
.inf-continue-btn:disabled {
    background: #999;
    cursor: not-allowed;
    transform: none;
}
.inf-continue-btn .inf-arrow {
    margin-left: 8px;
}

/* --- Footer --- */
.inf-footer {
    position: fixed; bottom: 0; left: 0; right: 0; z-index: 100;
    background: #F5F2ED;
    border-top: 1px solid #E0DCD6;
    padding: 10px 20px;
    text-align: center;
    font-size: 10px; color: #AAAAAA; font-family: Inter, sans-serif; line-height: 1.4;
}
.inf-footer a { color: #6B7F4E; text-decoration: none; }
</style>

<div class="inf-footer">
  Your responses are held strictly confidential.
  See our <a href="#">Privacy Policy</a>
  and commitment to research ethics and integrity.
</div>

<div id="inf-root">
  <div class="inf-logo-bar">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM">
  </div>

  <div class="inf-progress-wrap">
    <div class="inf-section-header">
      <span class="inf-section-title">SECTION 3: INFLUENCE &amp; ENGAGEMENT</span>
      <span class="inf-progress-label">1 of 3 Questions</span>
    </div>
    <div class="inf-progress-track"><div class="inf-progress-fill" style="width:33%"></div></div>
  </div>

  <div class="inf-intro-card">
    <p class="inf-intro-body">
      People shape decisions in different ways. The questions that follow
      help us understand how you tend to engage and where your influence
      shows up. At the end of the survey, we&rsquo;ll share a snapshot of your
      influence profile.
    </p>
  </div>

  <div class="inf-q-label">1 of 5 Questions</div>
  <div class="inf-card" id="inf1-card">
    <div class="inf-q-title">How have you made your voice heard?</div>
    <div class="inf-q-subtitle">Select all that apply. At the end, you&rsquo;ll see your Influence Compass.</div>

    <div id="inf1-checks">
      <div class="inf-check-row" data-row="r1">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">I&rsquo;ve worked inside government, held office, or staffed those who do</span>
      </div>
      <div class="inf-check-row" data-row="r2">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">I&rsquo;ve helped decide who runs for office or who gets hired in political or government roles</span>
      </div>
      <div class="inf-check-row" data-row="r3">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">I advise organizations, sit on boards, or have testified on policy matters</span>
      </div>
      <div class="inf-check-row" data-row="r4">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">I&rsquo;ve written, spoken publicly, or published on issues</span>
      </div>
      <div class="inf-check-row" data-row="r5">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">I&rsquo;ve organized people, worked campaigns, or rallied others to action</span>
      </div>
      <div class="inf-check-row" data-row="r6">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">I vote, follow current events, and stay involved in my community</span>
      </div>
    </div>

    <button type="button" class="inf-none-btn" id="inf1-none">None of these</button>
  </div>

  <div class="inf-continue-wrap">
    <button type="button" class="inf-continue-btn" id="inf1-continue" disabled>CONTINUE <span class="inf-arrow">&#8250;</span></button>
  </div>

</div>
]]></style>

  <style mode="after" name="question.footer"><![CDATA[
<script>
(function(){
  /* Guard: only run on INF1 page */
  if(!document.getElementById('inf1-card')) return;

  /* ---- find Decipher question element by label ---- */
  function findQ(q){
    var el = document.getElementById('q_'+q)
        || document.getElementById('question_'+q)
        || document.getElementById(q)
        || document.querySelector('[id$="_'+q+'"]');
    /* If we found an input rather than the question container, walk up */
    if(el && el.closest && !el.classList.contains('question')){
      var par = el.closest('.question');
      if(par) el = par;
    }
    return el;
  }

  /* ---- Debug: log all .question elements + their inputs ---- */
  var allQ = document.querySelectorAll('.question');
  console.log('[INF1 init] All .question IDs:', [].map.call(allQ, function(e){return e.id;}));
  var infBox = findQ('INF1');
  if(infBox){
    console.log('[INF1 init] Found INF1 id='+infBox.id);
    var allInputs = infBox.querySelectorAll('input');
    console.log('[INF1 init] INF1 has '+allInputs.length+' inputs:');
    [].forEach.call(allInputs, function(inp){
      console.log('  type='+inp.type+' name='+inp.name+' value='+inp.value+' checked='+inp.checked);
    });
  } else {
    console.warn('[INF1 init] INF1 NOT FOUND - dumping all questions with checkboxes:');
    allQ.forEach(function(q){
      var cbs = q.querySelectorAll('input[type="checkbox"]');
      if(cbs.length > 0){
        console.log('  '+q.id+' has '+cbs.length+' checkboxes');
        [].forEach.call(cbs, function(cb){
          console.log('    name='+cb.name+' value='+cb.value);
        });
      }
    });
  }

  /* ---- sync checkbox to native Decipher input ---- */
  function syncCheckbox(qLabel, rowLabel, checked){
    var box = findQ(qLabel);
    if(!box){ console.warn('[syncCB] '+qLabel+' NOT FOUND'); return; }
    var inp = null;

    /* Strategy 1: value matches row label */
    inp = box.querySelector('input[type="checkbox"][value="'+rowLabel+'"]');
    /* Strategy 2: name contains row label */
    if(!inp) inp = box.querySelector('input[type="checkbox"][name*="'+rowLabel+'"]');
    /* Strategy 3: position-based (r1=1st checkbox, r2=2nd, etc.) */
    if(!inp){
      var m = rowLabel.match(/^r(\d+)$/);
      if(m){
        var idx = parseInt(m[1], 10);
        var allCB = box.querySelectorAll('input[type="checkbox"]');
        if(idx === 99){
          /* r99 = exclusive/noanswer = usually last checkbox */
          for(var k=0; k<allCB.length; k++){
            var nm = (allCB[k].name||'')+(allCB[k].className||'');
            if(nm.indexOf('noanswer')!==-1||nm.indexOf('exclusive')!==-1){inp=allCB[k]; break;}
          }
          if(!inp && allCB.length>0) inp = allCB[allCB.length-1];
        } else if(idx>0 && idx<=allCB.length){
          inp = allCB[idx-1];
        }
      }
    }
    /* Strategy 4: scan ALL inputs */
    if(!inp){
      var all2 = box.querySelectorAll('input');
      for(var j=0; j<all2.length; j++){
        if(all2[j].value===rowLabel||(all2[j].name&&all2[j].name.indexOf(rowLabel)!==-1)){inp=all2[j]; break;}
      }
    }

    if(inp){
      console.log('[syncCB] '+qLabel+'.'+rowLabel+' checked='+checked+' (name='+inp.name+' val='+inp.value+' type='+inp.type+')');
      inp.checked = checked;
      inp.click();
      inp.dispatchEvent(new Event('change', {bubbles:true}));
      try{ jQuery(inp).trigger('click').trigger('change'); }catch(e){}
    } else {
      console.warn('[syncCB] '+qLabel+'.'+rowLabel+' NO INPUT FOUND in #'+box.id);
    }
  }

  /* ---- state tracking ---- */
  var selected = {};
  var noneSelected = false;

  var checkRows = document.querySelectorAll('#inf1-checks .inf-check-row');
  var noneBtn = document.getElementById('inf1-none');
  var continueBtn = document.getElementById('inf1-continue');

  function updateContinue(){
    var anyChecked = Object.keys(selected).some(function(k){ return selected[k]; });
    continueBtn.disabled = !(anyChecked || noneSelected);
  }

  /* Checkbox row click */
  checkRows.forEach(function(row){
    row.addEventListener('click', function(){
      var r = row.getAttribute('data-row');
      selected[r] = !selected[r];
      row.classList.toggle('inf-checked', selected[r]);
      syncCheckbox('INF1', r, selected[r]);

      if(selected[r] && noneSelected){
        noneSelected = false;
        noneBtn.classList.remove('inf-sel');
        syncCheckbox('INF1', 'r99', false);
      }
      updateContinue();
    });
  });

  /* None of these click */
  noneBtn.addEventListener('click', function(){
    noneSelected = !noneSelected;
    noneBtn.classList.toggle('inf-sel', noneSelected);
    syncCheckbox('INF1', 'r99', noneSelected);

    if(noneSelected){
      checkRows.forEach(function(row){
        var r = row.getAttribute('data-row');
        selected[r] = false;
        row.classList.remove('inf-checked');
        syncCheckbox('INF1', r, false);
      });
    }
    updateContinue();
  });

  /* Continue button */
  continueBtn.addEventListener('click', function(){
    if(continueBtn.disabled) return;
    console.log('[INF1 SUBMIT] selected=', JSON.stringify(selected), 'none=', noneSelected);

    /* Verify native inputs */
    var box = findQ('INF1');
    if(box){
      var chkd = box.querySelectorAll('input:checked');
      console.log('[INF1 SUBMIT] Native checked: '+chkd.length);
      [].forEach.call(chkd, function(c){ console.log('  '+c.name+'='+c.value); });

      /* Emergency fallback: if nothing checked natively, force-set by position */
      if(chkd.length === 0){
        console.warn('[INF1 SUBMIT] FALLBACK: force-setting by position');
        var allCB = box.querySelectorAll('input[type="checkbox"]');
        console.log('[INF1 SUBMIT] Found '+allCB.length+' native checkboxes');
        Object.keys(selected).forEach(function(r){
          if(!selected[r]) return;
          var m2 = r.match(/^r(\d+)$/);
          if(m2){
            var i2 = parseInt(m2[1],10) - 1;
            if(i2 >= 0 && i2 < allCB.length){
              allCB[i2].checked = true;
              allCB[i2].click();
              try{ jQuery(allCB[i2]).trigger('click').trigger('change'); }catch(e){}
              console.log('[INF1 SUBMIT] Force-set idx='+i2+' name='+allCB[i2].name);
            }
          }
        });
        if(noneSelected && allCB.length > 0){
          var last = allCB[allCB.length-1];
          last.checked = true; last.click();
          try{ jQuery(last).trigger('click').trigger('change'); }catch(e){}
          console.log('[INF1 SUBMIT] Force-set None (last)');
        }
      }
    }

    var nxt = document.querySelector('.nextPage, .button[type="submit"], input[type="submit"]');
    if(!nxt) nxt = document.querySelector('.button, input.button');
    if(nxt){
      nxt.setAttribute('style','display:block !important;visibility:hidden;position:absolute;left:-9999px;');
      console.log('[INF1 SUBMIT] Clicking native button:', nxt.tagName, nxt.className);
      nxt.click();
    } else {
      console.warn('[INF1 SUBMIT] No native button - form.submit()');
      var form = document.querySelector('form');
      if(form) form.submit();
    }
  });
})();
</script>
]]></style>

</checkbox>
<suspend/>

<note>===================== PAGE GROUP 6 -- INFLUENCE360_SCREEN2 =====================</note>

<checkbox
  label="INF2"
  atleast="0"
  optional="1"
  cond="INF1.r2"
  ss:questionClassNames="inf_hidden">
  <title>Which of these have you done, if any?</title>
  <row label="r1">Helped recruit a candidate to run for public office</row>
  <row label="r2">Helped recruit staff for a political campaign, office of an elected official, or state/local government appointment</row>
  <row label="r99" exclusive="1">None of these</row>

  <style mode="before" name="question.header"><![CDATA[
<style>
/* --- hide native Decipher question elements on influence page 2 --- */
.inf_hidden {
    border: none !important;
    box-shadow: none !important;
    background: transparent !important;
    padding: 0 !important;
    margin: 0 !important;
    min-height: 0 !important;
    overflow: visible !important;
}
.inf_hidden .question-text,
.inf_hidden .comment,
.inf_hidden .instruction-text,
.inf_hidden .answers-list,
.inf_hidden .fir-row,
.inf_hidden .row-elements,
.inf_hidden .noanswer-row,
.inf_hidden .error,
.inf_hidden .mobile-grid-wrapper,
.inf_hidden .grid-table-mode,
.inf_hidden .cell.noAnswer,
.inf_hidden .col-legend-group,
.inf_hidden .grouping-box,
.inf_hidden table {
    display: none !important;
}
/* hide native cardrating.1 widget elements */
.inf_hidden .card-slider,
.inf_hidden .card-slider-container,
.inf_hidden .card-container,
.inf_hidden .card-rating,
.inf_hidden .card-rating-container,
.inf_hidden .cardRating,
.inf_hidden .cards-wrapper,
.inf_hidden .card-nav,
.inf_hidden .card-buttons,
.inf_hidden .survey-card,
.inf_hidden .rating-scale,
.inf_hidden .card-item,
.inf_hidden .card-deck,
.inf_hidden .card-content,
.inf_hidden .card-footer,
.inf_hidden .zc-card,
.inf_hidden .zc-cardrating,
.inf_hidden > div:not(.inf-card):not(.inf-q-label):not(.inf-continue-wrap):not(.inf-logo-bar):not(.inf-progress-wrap):not(.inf-footer) {
    display: none !important;
}
/* hide native Next/submit button */
.nextPage, .button[type="submit"], input[type="submit"] {
    display: none !important;
}

/* --- INF2 root container --- */
#inf2-root {
    max-width: 640px;
    margin: 0 auto;
    padding: 24px 16px 120px;
    font-family: Inter, sans-serif;
    color: #333333;
}

/* --- PRISM logo --- */
.inf-logo-bar {
    text-align: center;
    padding: 4px 0 36px;
}
.inf-logo-bar img {
    height: 40px;
    width: auto;
}

/* --- Section header + progress --- */
.inf-progress-wrap {
    position: sticky;
    top: 0; z-index: 200;
    background: #F5F2ED;
    padding: 8px 0 12px;
}
.inf-section-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 6px;
    font-family: Inter, sans-serif;
}
.inf-section-title {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: .06em;
    text-transform: uppercase;
    color: #888;
    text-decoration: underline;
    text-underline-offset: 2px;
}
.inf-progress-label {
    font-size: 9px;
    letter-spacing: .05em;
    color: #888888;
    font-weight: 600;
    text-transform: uppercase;
}
.inf-progress-track {
    height: 5px;
    background: #E0E0E0;
    border-radius: 3px;
}
.inf-progress-fill {
    height: 100%;
    width: 33%;
    background: #6B7F4E;
    border-radius: 3px;
    transition: width .45s cubic-bezier(.4,0,.2,1);
}

/* --- Question card --- */
.inf-card {
    background: #FFFFFF;
    border: 1px solid #E0DCD6;
    border-radius: 10px;
    padding: 22px 24px 20px;
    margin-bottom: 28px;
}
.inf-q-label {
    font-family: Inter, sans-serif;
    font-size: 9px; font-weight: 600;
    letter-spacing: .08em; text-transform: uppercase;
    color: #888888; margin-bottom: 4px;
    text-align: right;
}
.inf-q-title {
    font-family: Fraunces, Georgia, serif;
    font-size: 19px; font-weight: 700;
    color: #333333; line-height: 1.42;
    margin-bottom: 6px;
}
.inf-q-subtitle {
    font-family: Inter, sans-serif;
    font-size: 12px; color: #888888;
    margin-bottom: 18px;
}

/* --- Checklist checkbox rows --- */
.inf-check-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 12px 14px;
    border-bottom: 1px solid #F0EDE8;
    cursor: pointer;
    user-select: none;
    transition: background .15s;
}
.inf-check-row:last-of-type {
    border-bottom: none;
}
.inf-check-row:hover {
    background: #FAFAF8;
}
.inf-check-box {
    flex-shrink: 0;
    width: 22px; height: 22px;
    border: 2px solid #CCCCCC;
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: border-color .15s, background .15s;
    margin-top: 1px;
}
.inf-check-row.inf-checked .inf-check-box {
    border-color: #6B7F4E;
    background: #6B7F4E;
}
.inf-check-box svg {
    display: none;
    width: 14px; height: 14px;
}
.inf-check-row.inf-checked .inf-check-box svg {
    display: block;
}
.inf-check-label {
    font-family: Inter, sans-serif;
    font-size: 14px;
    color: #333333;
    line-height: 1.45;
    flex: 1;
}

/* --- "None of these" button --- */
.inf-none-btn {
    display: inline-block;
    padding: 8px 18px;
    border: 1px solid #333;
    border-radius: 6px;
    background: #fff;
    font-family: Inter, sans-serif;
    font-size: 13px;
    font-weight: 600;
    color: #333;
    cursor: pointer;
    margin-top: 14px;
    transition: background .15s, border-color .15s;
}
.inf-none-btn:hover {
    background: #F5F2ED;
}
.inf-none-btn.inf-sel {
    background: #333;
    color: #fff;
}

/* --- Button-select grid (INF7 followers) --- */
.inf-btn-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    margin-top: 8px;
}
.inf-btn-option {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 14px 8px;
    border: 2px solid #E0DCD6;
    border-radius: 10px;
    background: #fff;
    cursor: pointer;
    transition: border-color .18s, background .18s;
    user-select: none;
    text-align: center;
}
.inf-btn-option:hover {
    border-color: #6B7F4E;
}
.inf-btn-option.inf-sel {
    border-color: #6B7F4E;
    background: #EAF0E0;
}
.inf-btn-big {
    font-family: Inter, sans-serif;
    font-size: 26px;
    font-weight: 800;
    color: #333;
    line-height: 1.1;
}
.inf-btn-range {
    font-family: Inter, sans-serif;
    font-size: 11px;
    font-weight: 500;
    color: #999;
    margin-top: 2px;
}

/* --- Card-swipe UI (INF8 social media activity) --- */
.inf-swipe-wrap {
    position: relative;
    margin-bottom: 20px;
}
.inf-swipe-card-area {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 16px;
}
.inf-swipe-card {
    flex: 1;
    background: #F8F5F0;
    border: 1px solid #E0DCD6;
    border-radius: 24px;
    padding: 20px 24px;
    min-height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    font-family: Inter, sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: #333;
    line-height: 1.4;
}
/* --- Green highlight flash for selections --- */
.inf-btn-option.inf-flash {
    border-color: #6B7F4E;
    background: #D4E8B8;
    transition: background .12s;
}
.inf-freq-btn.inf-flash {
    border-color: #6B7F4E;
    background: #D4E8B8;
    color: #333;
    transition: background .12s;
}
.inf-swipe-peek {
    display: flex;
    justify-content: space-between;
    margin-bottom: 10px;
    font-family: Inter, sans-serif;
    font-size: 11px;
    color: #AAA;
}
.inf-swipe-peek-left,
.inf-swipe-peek-right {
    max-width: 40%;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}
.inf-freq-btns {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
    justify-content: center;
}
.inf-freq-btn {
    padding: 8px 12px;
    border: 1px solid #E0DCD6;
    border-radius: 6px;
    background: #fff;
    font-family: Inter, sans-serif;
    font-size: 12px;
    font-weight: 500;
    color: #333;
    cursor: pointer;
    transition: border-color .15s, background .15s;
}
.inf-freq-btn:hover {
    border-color: #6B7F4E;
}
.inf-freq-btn.inf-sel {
    border-color: #6B7F4E;
    background: #6B7F4E;
    color: #fff;
}
.inf-swipe-dots {
    display: flex;
    justify-content: center;
    gap: 6px;
    margin-top: 12px;
}
.inf-swipe-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #E0DCD6;
    transition: background .2s;
}
.inf-swipe-dot.inf-active {
    background: #6B7F4E;
}
.inf-swipe-dot.inf-done {
    background: #6B7F4E;
}

/* --- Continue button --- */
.inf-continue-wrap {
    text-align: center;
    padding: 24px 0 12px;
}
.inf-continue-btn {
    background: #6B7F4E;
    color: #fff; border: none;
    border-radius: 24px;
    padding: 14px 36px;
    font-family: Inter, sans-serif;
    font-size: 14px; font-weight: 700;
    letter-spacing: .08em; text-transform: uppercase;
    cursor: pointer;
    transition: background .2s, transform .15s;
}
.inf-continue-btn:hover {
    background: #5A6B42;
    transform: translateY(-1px);
}
.inf-continue-btn:disabled {
    background: #999;
    cursor: not-allowed;
    transform: none;
}

/* --- Footer --- */
.inf-footer {
    position: fixed; bottom: 0; left: 0; right: 0; z-index: 100;
    background: #F5F2ED;
    border-top: 1px solid #E0DCD6;
    padding: 10px 20px;
    text-align: center;
    font-size: 10px; color: #AAAAAA; font-family: Inter, sans-serif; line-height: 1.4;
}
.inf-footer a { color: #6B7F4E; text-decoration: none; }
</style>

<div class="inf-footer">
  Your responses are held strictly confidential.
  See our <a href="#">Privacy Policy</a>
  and commitment to research ethics and integrity.
</div>

<div id="inf2-root">
  <div class="inf-logo-bar">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM">
  </div>

  <div class="inf-progress-wrap" id="inf2-progress-wrap">
    <div class="inf-section-header">
      <span class="inf-section-title">SECTION 3: INFLUENCE &amp; ENGAGEMENT</span>
      <span class="inf-progress-label" id="inf2-progress-label">1 of 4 Questions</span>
    </div>
    <div class="inf-progress-track"><div class="inf-progress-fill" id="inf2-progress-fill" style="width:25%"></div></div>
  </div>

  <!-- Merged checklist card: INF2-INF5 rows in one card -->
  <div class="inf-q-label">1 of 4 Questions</div>
  <div class="inf-card" id="inf-merged-checklist">
    <div class="inf-q-title">Which of these have you done, if any?</div>
    <div class="inf-q-subtitle">Select all that apply. At the end, you&rsquo;ll see your Influence Compass.</div>
    <div id="inf-merged-checks">
      <div class="inf-check-row" data-q="INF2" data-row="r1">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Helped recruit a candidate to run for public office</span>
      </div>
      <div class="inf-check-row" data-q="INF2" data-row="r2">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Helped recruit staff for a political campaign, office of an elected official, or state/local government appointment</span>
      </div>
      <div class="inf-check-row" data-q="INF3" data-row="r1">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Met (in person or video) with congressional, agency, or state legislative staff</span>
      </div>
      <div class="inf-check-row" data-q="INF3" data-row="r2">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Sat on the board or executive committee of any health, policy, or business association</span>
      </div>
      <div class="inf-check-row" data-q="INF3" data-row="r3">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Worked with or advised a Washington-based organization (think tank, patient advocacy group, trade association)</span>
      </div>
      <div class="inf-check-row" data-q="INF3" data-row="r4">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Testified or submitted written comments to a state legislative or Congressional committee</span>
      </div>
      <div class="inf-check-row" data-q="INF4" data-row="r1">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Spoke at a public meeting, protest, webinar, church or local group on a health topic</span>
      </div>
      <div class="inf-check-row" data-q="INF5" data-row="r1">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Volunteered on a political campaign</span>
      </div>
      <div class="inf-check-row" data-q="INF5" data-row="r2">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Organized an event or rally on any issue</span>
      </div>
    </div>
    <button type="button" class="inf-none-btn" id="inf-merged-none">None of these</button>
  </div>

  <!-- INF6 card: separate card for policy actions -->
  <div class="inf-q-label">2 of 4 Questions</div>
  <div class="inf-card" id="inf6-card">
    <div class="inf-q-title">In the past 12 months, have you taken any of the following actions related to a public policy issue?</div>
    <div class="inf-q-subtitle">Select all that apply.</div>
    <div id="inf6-checks">
      <div class="inf-check-row" data-q="INF6" data-row="r1">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Signed a petition</span>
      </div>
      <div class="inf-check-row" data-q="INF6" data-row="r2">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Attended a meeting</span>
      </div>
      <div class="inf-check-row" data-q="INF6" data-row="r3">
        <div class="inf-check-box"><svg viewBox="0 0 14 14" fill="none"><polyline points="2,7 6,11 12,3" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span class="inf-check-label">Donated money</span>
      </div>
    </div>
    <button type="button" class="inf-none-btn" id="inf6-none">None of these</button>
  </div>
]]></style>
</checkbox>

<checkbox
  label="INF3"
  atleast="0"
  optional="1"
  cond="INF1.r3"
  ss:questionClassNames="inf_hidden">
  <title>Which of these have you done, if any?</title>
  <row label="r1">Met (in person or video) with congressional, agency, or state legislative staff</row>
  <row label="r2">Sat on the board or executive committee of any health, policy, or business association</row>
  <row label="r3">Worked with or advised a Washington-based organization (think tank, patient advocacy group, trade association)</row>
  <row label="r4">Testified or submitted written comments to a state legislative or Congressional committee</row>
  <row label="r99" exclusive="1">None of these</row>

  <style mode="before" name="question.header"><![CDATA[
  <!-- Rows in merged checklist -->
]]></style>
</checkbox>

<checkbox
  label="INF4"
  atleast="0"
  optional="1"
  cond="INF1.r4"
  ss:questionClassNames="inf_hidden">
  <title>Which of these have you done, if any?</title>
  <row label="r1">Spoke at a public meeting, protest, webinar, church or local group on a health topic</row>
  <row label="r99" exclusive="1">None of these</row>

  <style mode="before" name="question.header"><![CDATA[
  <!-- Rows in merged checklist -->
]]></style>
</checkbox>

<checkbox
  label="INF5"
  atleast="0"
  optional="1"
  cond="INF1.r5"
  ss:questionClassNames="inf_hidden">
  <title>Which of these have you done, if any?</title>
  <row label="r1">Volunteered on a political campaign</row>
  <row label="r2">Organized an event or rally on any issue</row>
  <row label="r99" exclusive="1">None of these</row>

  <style mode="before" name="question.header"><![CDATA[
  <!-- Rows in merged checklist -->
]]></style>
</checkbox>

<checkbox
  label="INF6"
  atleast="0"
  optional="1"
  ss:questionClassNames="inf_hidden">
  <title>In the past 12 months, have you taken any of the following actions related to a public policy issue?</title>
  <row label="r1">Signed a petition</row>
  <row label="r2">Attended a meeting</row>
  <row label="r3">Donated money</row>
  <row label="r99" exclusive="1">None of these</row>

  <style mode="before" name="question.header"><![CDATA[
  <!-- Rows in merged checklist -->
]]></style>
</checkbox>

<radio
  label="INF7"
  optional="0"
  ss:questionClassNames="inf_hidden">
  <title>Across all your public social-media accounts about how many followers or subscribers do you have in total?</title>
  <comment>Including from X/Twitter, Instagram, TikTok, Facebook pages, Substack, podcast, YouTube, etc.</comment>
  <row label="r1" value="1">0 / None</row>
  <row label="r2" value="2">&lt;500</row>
  <row label="r3" value="3">500 - 2,000</row>
  <row label="r4" value="4">2,000 - 10,000</row>
  <row label="r5" value="5">10,000 - 50,000</row>
  <row label="r6" value="6">50,000+</row>

  <style mode="before" name="question.header"><![CDATA[
  <!-- INF7 card: Social media followers (always shown) -->
  <div class="inf-q-label">3 of 4 Questions</div>
  <div class="inf-card inf2-card" data-q="INF7" id="inf2-card-INF7">
    <div class="inf-q-title">Across all your public social-media accounts about how many followers or subscribers do you have in total?</div>
    <div class="inf-q-subtitle">Including from X/Twitter, Instagram, TikTok, Facebook pages, Substack, podcast, YouTube, etc.</div>
    <div class="inf-btn-grid" data-q="INF7">
      <div class="inf-btn-option" data-row="r1" data-val="1">
        <span class="inf-btn-big">0</span>
        <span class="inf-btn-range">None</span>
      </div>
      <div class="inf-btn-option" data-row="r2" data-val="2">
        <span class="inf-btn-big">&lt;500</span>
      </div>
      <div class="inf-btn-option" data-row="r3" data-val="3">
        <span class="inf-btn-big">500</span>
        <span class="inf-btn-range">to 2,000</span>
      </div>
      <div class="inf-btn-option" data-row="r4" data-val="4">
        <span class="inf-btn-big">2,000</span>
        <span class="inf-btn-range">to 10,000</span>
      </div>
      <div class="inf-btn-option" data-row="r5" data-val="5">
        <span class="inf-btn-big">10,000</span>
        <span class="inf-btn-range">to 50,000</span>
      </div>
      <div class="inf-btn-option" data-row="r6" data-val="6">
        <span class="inf-btn-big">50,000+</span>
      </div>
    </div>
  </div>
]]></style>
</radio>

<radio
  label="INF8"
  type="rating"
  values="order"
  shuffle="rows"
  optional="0"
  ss:questionClassNames="inf_hidden">
  <title>How many times over the past 30 days have you...</title>
  <comment>Across social media platforms you use (X/Twitter, Instagram, TikTok, Facebook, Substack, YouTube, etc.). Approximately how many times have you done any of the following to advocate on a public policy issue?</comment>
  <row label="r1">Created an original post or video</row>
  <row label="r2">Shared or reposted content</row>
  <row label="r3">Commented or replied to posts</row>
  <col label="c1" value="1">Never</col>
  <col label="c2" value="2">Not in the past 30 days</col>
  <col label="c3" value="3">One time</col>
  <col label="c4" value="4">2 - 4 times</col>
  <col label="c5" value="5">5+ times</col>

  <style mode="before" name="question.header"><![CDATA[
  <!-- INF8 card: Social media activity card-swipe (always shown) -->
  <div class="inf-q-label">4 of 4 Questions</div>
  <div class="inf-card inf2-card" data-q="INF8" id="inf2-card-INF8">
    <div class="inf-q-title">How many times over the past 30 days have you&hellip;</div>
    <div class="inf-q-subtitle">Across social media platforms you use (X/Twitter, Instagram, TikTok, Facebook, Substack, YouTube, etc.). Approximately how many times have you done any of the following to advocate on a public policy issue?</div>

    <div class="inf-swipe-wrap" id="inf8-swipe">
      <div class="inf-swipe-peek">
        <span class="inf-swipe-peek-left" id="inf8-peek-left"></span>
        <span class="inf-swipe-peek-right" id="inf8-peek-right"></span>
      </div>
      <div class="inf-swipe-card-area">
        <div class="inf-swipe-card" id="inf8-card-text">Created an original post or video</div>
      </div>
      <div class="inf-freq-btns" id="inf8-freq-btns">
        <button type="button" class="inf-freq-btn" data-col="c1" data-val="1">Never</button>
        <button type="button" class="inf-freq-btn" data-col="c2" data-val="2">Not in the<br>past 30 days</button>
        <button type="button" class="inf-freq-btn" data-col="c3" data-val="3">One time</button>
        <button type="button" class="inf-freq-btn" data-col="c4" data-val="4">2 - 4 times</button>
        <button type="button" class="inf-freq-btn" data-col="c5" data-val="5">5+ times</button>
      </div>
      <div class="inf-swipe-dots" id="inf8-dots">
        <div class="inf-swipe-dot inf-active" data-idx="0"></div>
        <div class="inf-swipe-dot" data-idx="1"></div>
        <div class="inf-swipe-dot" data-idx="2"></div>
      </div>
    </div>
  </div>

  <div class="inf-continue-wrap" id="inf2-continue-wrap">
    <button type="button" class="inf-continue-btn" id="inf2-continue" disabled>CONTINUE <span class="inf-arrow">&#8250;</span></button>
  </div>
</div>
]]></style>

  <style mode="after" name="question.footer"><![CDATA[
<script>
(function(){
  /* Guard: only run on page.group.6 */
  if(!document.getElementById('inf2-root')) return;

  /* ---- find Decipher question element by label ---- */
  function findQ(q){
    var el = document.getElementById('q_'+q)
        || document.getElementById('question_'+q)
        || document.getElementById(q)
        || document.querySelector('[id$="_'+q+'"]');
    if(el && el.closest && !el.classList.contains('question')){
      var par = el.closest('.question');
      if(par) el = par;
    }
    return el;
  }

  /* ---- Debug init ---- */
  var allQ = document.querySelectorAll('.question');
  console.log('[INF2-page init] All .question IDs:', [].map.call(allQ, function(e){return e.id;}));
  ['INF2','INF3','INF4','INF5','INF6','INF7','INF8'].forEach(function(q){
    var el = findQ(q);
    if(el){
      var inputs = el.querySelectorAll('input');
      console.log('[INF2-page init] '+q+' found id='+el.id+' inputs='+inputs.length);
      [].forEach.call(inputs, function(inp){
        console.log('  type='+inp.type+' name='+inp.name+' value='+inp.value);
      });
    } else {
      console.log('[INF2-page init] '+q+' NOT in DOM (cond=false or missing)');
    }
  });

  /* ---- sync checkbox to native Decipher input ---- */
  function syncCheckbox(qLabel, rowLabel, checked){
    var box = findQ(qLabel);
    if(!box){ console.warn('[syncCB] '+qLabel+' NOT FOUND'); return; }
    var inp = null;

    inp = box.querySelector('input[type="checkbox"][value="'+rowLabel+'"]');
    if(!inp) inp = box.querySelector('input[type="checkbox"][name*="'+rowLabel+'"]');
    if(!inp){
      var m = rowLabel.match(/^r(\d+)$/);
      if(m){
        var idx = parseInt(m[1], 10);
        var allCB = box.querySelectorAll('input[type="checkbox"]');
        if(idx === 99){
          for(var k=0; k<allCB.length; k++){
            var nm = (allCB[k].name||'')+(allCB[k].className||'');
            if(nm.indexOf('noanswer')!==-1||nm.indexOf('exclusive')!==-1){inp=allCB[k]; break;}
          }
          if(!inp && allCB.length>0) inp = allCB[allCB.length-1];
        } else if(idx>0 && idx<=allCB.length){
          inp = allCB[idx-1];
        }
      }
    }
    if(!inp){
      var all2 = box.querySelectorAll('input');
      for(var j=0; j<all2.length; j++){
        if(all2[j].value===rowLabel||(all2[j].name&&all2[j].name.indexOf(rowLabel)!==-1)){inp=all2[j]; break;}
      }
    }

    if(inp){
      console.log('[syncCB] '+qLabel+'.'+rowLabel+' checked='+checked+' (name='+inp.name+' val='+inp.value+')');
      inp.checked = checked;
      inp.click();
      inp.dispatchEvent(new Event('change', {bubbles:true}));
      try{ jQuery(inp).trigger('click').trigger('change'); }catch(e){}
    } else {
      console.warn('[syncCB] '+qLabel+'.'+rowLabel+' NO INPUT FOUND');
    }
  }

  /* ---- sync radio to native Decipher input ---- */
  function syncRadio(qLabel, val){
    var box = findQ(qLabel);
    if(!box){ console.warn('[syncRadio] '+qLabel+' NOT FOUND'); return; }
    var all = box.querySelectorAll('input[type="radio"]');
    var inp = null;
    for(var i=0; i<all.length; i++){
      if(all[i].value === val || all[i].value === 'r'+val){ inp = all[i]; break; }
    }
    if(!inp){
      var idx = parseInt(val,10);
      if(idx>0 && idx<=all.length) inp = all[idx-1];
    }
    if(inp){
      console.log('[syncRadio] '+qLabel+' val='+val+' (name='+inp.name+' value='+inp.value+')');
      inp.checked = true;
      inp.click();
      inp.dispatchEvent(new Event('change', {bubbles:true}));
      try{ jQuery(inp).trigger('click').trigger('change'); }catch(e){}
    } else {
      console.warn('[syncRadio] '+qLabel+' val='+val+' NOT FOUND');
    }
  }

  /* ---- sync card-rating to native Decipher input ---- */
  function syncRating(qLabel, rowLabel, colVal){
    var box = findQ(qLabel);
    if(!box){ console.warn('[syncRating] '+qLabel+' NOT FOUND'); return; }
    var all = box.querySelectorAll('input[type="radio"]');
    var inp = null;
    for(var i=0; i<all.length; i++){
      var name = all[i].name || '';
      var v = all[i].value;
      if(name.indexOf(rowLabel) !== -1 && (v === colVal || v === 'c'+colVal)){ inp = all[i]; break; }
    }
    if(!inp){
      /* Try: find row container, then radio by value */
      var rowEls = box.querySelectorAll('[class*="'+rowLabel+'"], [data-row="'+rowLabel+'"], tr[id*="'+rowLabel+'"]');
      for(var r=0; r<rowEls.length && !inp; r++){
        var radios = rowEls[r].querySelectorAll('input[type="radio"]');
        for(var j=0; j<radios.length; j++){
          if(radios[j].value === colVal || radios[j].value === 'c'+colVal){ inp = radios[j]; break; }
        }
      }
    }
    if(!inp){
      /* Position-based fallback: group radios by name, find row group, select by col index */
      var groups = {};
      for(var k=0; k<all.length; k++){
        var n = all[k].name;
        if(!groups[n]) groups[n] = [];
        groups[n].push(all[k]);
      }
      var gKeys = Object.keys(groups);
      var rowMatch = rowLabel.match(/^r(\d+)$/);
      var colMatch = colVal.match ? colVal.match(/^(\d+)$/) : null;
      if(rowMatch && gKeys.length >= parseInt(rowMatch[1],10)){
        var rowGroup = groups[gKeys[parseInt(rowMatch[1],10)-1]];
        var cIdx = colMatch ? parseInt(colMatch[1],10)-1 : parseInt(colVal,10)-1;
        if(rowGroup && cIdx >= 0 && cIdx < rowGroup.length){
          inp = rowGroup[cIdx];
        }
      }
    }

    if(inp){
      console.log('[syncRating] '+qLabel+' '+rowLabel+'='+colVal+' (name='+inp.name+' value='+inp.value+')');
      inp.checked = true;
      inp.click();
      inp.dispatchEvent(new Event('change', {bubbles:true}));
      try{ jQuery(inp).trigger('click').trigger('change'); }catch(e){}
    } else {
      console.warn('[syncRating] '+qLabel+' '+rowLabel+'='+colVal+' NOT FOUND');
    }
  }

  /* ==================================================================
     MERGED CHECKLIST + 3-CARD LAYOUT (no progressive disclosure)
     ================================================================== */

  var MERGED_LABELS = ['INF2','INF3','INF4','INF5'];
  var visibleCBs = [];
  MERGED_LABELS.forEach(function(q){
    if(findQ(q)){
      visibleCBs.push(q);
      console.log('[MERGED] '+q+' in DOM');
    } else {
      console.log('[MERGED] '+q+' NOT in DOM (cond=false)');
      document.querySelectorAll('#inf-merged-checks .inf-check-row[data-q="'+q+'"]').forEach(function(r){
        r.style.display = 'none';
      });
    }
  });

  /* INF6 is always visible (no condition) but track separately */
  var inf6Visible = !!findQ('INF6');

  /* Track 4 cards */
  var cardAnswered = { checklist: false, INF6: false, INF7: false, INF8: false };
  var hasAnyRows = document.querySelectorAll('#inf-merged-checks .inf-check-row:not([style*="display: none"])').length > 0;
  if(!hasAnyRows) cardAnswered.checklist = true;
  if(!inf6Visible) cardAnswered.INF6 = true;

  function updateContinue(){
    var allDone = cardAnswered.checklist && cardAnswered.INF6 && cardAnswered.INF7 && cardAnswered.INF8;
    document.getElementById('inf2-continue').disabled = !allDone;
  }

  /* ==================================================================
     MERGED CHECKLIST
     ================================================================== */
  var mergedSelected = {};
  var mergedNone = false;
  var noneBtn = document.getElementById('inf-merged-none');

  document.querySelectorAll('#inf-merged-checks .inf-check-row').forEach(function(row){
    if(row.style.display === 'none') return;
    row.addEventListener('click', function(){
      var q = row.getAttribute('data-q');
      var r = row.getAttribute('data-row');
      var key = q+'_'+r;
      mergedSelected[key] = !mergedSelected[key];
      row.classList.toggle('inf-checked', mergedSelected[key]);
      syncCheckbox(q, r, mergedSelected[key]);

      if(mergedSelected[key] && mergedNone){
        mergedNone = false;
        if(noneBtn) noneBtn.classList.remove('inf-sel');
        visibleCBs.forEach(function(qq){ syncCheckbox(qq, 'r99', false); });
      }

      cardAnswered.checklist = mergedNone || Object.keys(mergedSelected).some(function(k){ return mergedSelected[k]; });
      updateContinue();
    });
  });

  if(noneBtn){
    noneBtn.addEventListener('click', function(){
      mergedNone = !mergedNone;
      noneBtn.classList.toggle('inf-sel', mergedNone);
      visibleCBs.forEach(function(q){ syncCheckbox(q, 'r99', mergedNone); });

      if(mergedNone){
        document.querySelectorAll('#inf-merged-checks .inf-check-row').forEach(function(row){
          if(row.style.display === 'none') return;
          var q2 = row.getAttribute('data-q');
          var r2 = row.getAttribute('data-row');
          mergedSelected[q2+'_'+r2] = false;
          row.classList.remove('inf-checked');
          syncCheckbox(q2, r2, false);
        });
      }

      cardAnswered.checklist = mergedNone || Object.keys(mergedSelected).some(function(k){ return mergedSelected[k]; });
      updateContinue();
    });
  }

  /* ==================================================================
     INF6 SEPARATE CARD
     ================================================================== */
  var inf6Selected = {};
  var inf6None = false;
  var inf6NoneBtn = document.getElementById('inf6-none');

  document.querySelectorAll('#inf6-checks .inf-check-row').forEach(function(row){
    row.addEventListener('click', function(){
      var r = row.getAttribute('data-row');
      inf6Selected[r] = !inf6Selected[r];
      row.classList.toggle('inf-checked', inf6Selected[r]);
      syncCheckbox('INF6', r, inf6Selected[r]);

      if(inf6Selected[r] && inf6None){
        inf6None = false;
        if(inf6NoneBtn) inf6NoneBtn.classList.remove('inf-sel');
        syncCheckbox('INF6', 'r99', false);
      }

      cardAnswered.INF6 = inf6None || Object.keys(inf6Selected).some(function(k){ return inf6Selected[k]; });
      updateContinue();
    });
  });

  if(inf6NoneBtn){
    inf6NoneBtn.addEventListener('click', function(){
      inf6None = !inf6None;
      inf6NoneBtn.classList.toggle('inf-sel', inf6None);
      syncCheckbox('INF6', 'r99', inf6None);

      if(inf6None){
        document.querySelectorAll('#inf6-checks .inf-check-row').forEach(function(row){
          var r2 = row.getAttribute('data-row');
          inf6Selected[r2] = false;
          row.classList.remove('inf-checked');
          syncCheckbox('INF6', r2, false);
        });
      }

      cardAnswered.INF6 = inf6None || Object.keys(inf6Selected).some(function(k){ return inf6Selected[k]; });
      updateContinue();
    });
  }

  /* ==================================================================
     BUTTON-SELECT GRID (INF7)
     ================================================================== */

  var inf7Grid = document.querySelector('.inf-btn-grid[data-q="INF7"]');
  if(inf7Grid){
    inf7Grid.querySelectorAll('.inf-btn-option').forEach(function(opt){
      opt.addEventListener('click', function(){
        inf7Grid.querySelectorAll('.inf-btn-option').forEach(function(o){ o.classList.remove('inf-sel','inf-flash'); });
        opt.classList.add('inf-flash');
        var val = opt.getAttribute('data-val');
        syncRadio('INF7', val);
        cardAnswered.INF7 = true;
        updateContinue();
        setTimeout(function(){
          opt.classList.remove('inf-flash');
          opt.classList.add('inf-sel');
        }, 400);
      });
    });
  }

  /* ==================================================================
     CARD-SWIPE RATING (INF8)
     ================================================================== */

  var swipeItems = [
    {row: 'r1', text: 'Created an original post or video'},
    {row: 'r2', text: 'Shared or reposted content'},
    {row: 'r3', text: 'Commented or replied to posts'}
  ];
  var swipeIdx = 0;
  var swipeAnswers = {};
  var cardText = document.getElementById('inf8-card-text');
  var peekLeft = document.getElementById('inf8-peek-left');
  var peekRight = document.getElementById('inf8-peek-right');
  var freqBtns = document.getElementById('inf8-freq-btns');
  var dotsEl = document.getElementById('inf8-dots');

  function updateSwipe(){
    if(!cardText) return;
    cardText.textContent = swipeItems[swipeIdx].text;
    if(peekLeft) peekLeft.textContent = swipeIdx > 0 ? swipeItems[swipeIdx-1].text : '';
    if(peekRight) peekRight.textContent = swipeIdx < swipeItems.length-1 ? swipeItems[swipeIdx+1].text : '';
    if(freqBtns){
      var cur = swipeAnswers[swipeItems[swipeIdx].row];
      freqBtns.querySelectorAll('.inf-freq-btn').forEach(function(b){
        b.classList.remove('inf-flash');
        b.classList.toggle('inf-sel', b.getAttribute('data-col') === cur);
      });
    }
    if(dotsEl){
      dotsEl.querySelectorAll('.inf-swipe-dot').forEach(function(dot, i){
        dot.classList.toggle('inf-active', i === swipeIdx);
        dot.classList.toggle('inf-done', !!swipeAnswers[swipeItems[i].row]);
      });
    }
  }

  if(freqBtns){
    freqBtns.querySelectorAll('.inf-freq-btn').forEach(function(btn){
      btn.addEventListener('click', function(){
        var col = btn.getAttribute('data-col');
        var val = btn.getAttribute('data-val');
        swipeAnswers[swipeItems[swipeIdx].row] = col;
        syncRating('INF8', swipeItems[swipeIdx].row, val);
        var allRated = swipeItems.every(function(item){ return swipeAnswers[item.row]; });
        cardAnswered.INF8 = allRated;

        /* Green flash highlight then auto-advance */
        freqBtns.querySelectorAll('.inf-freq-btn').forEach(function(b){ b.classList.remove('inf-sel','inf-flash'); });
        btn.classList.add('inf-flash');
        setTimeout(function(){
          btn.classList.remove('inf-flash');
          btn.classList.add('inf-sel');
          if(swipeIdx < swipeItems.length - 1){
            setTimeout(function(){ swipeIdx++; updateSwipe(); }, 150);
          } else {
            updateSwipe();
          }
          updateContinue();
        }, 400);
      });
    });
  }

  updateSwipe();

  /* ==================================================================
     CONTINUE BUTTON
     ================================================================== */

  var continueBtn = document.getElementById('inf2-continue');
  if(continueBtn){
    continueBtn.addEventListener('click', function(){
      if(continueBtn.disabled) return;
      console.log('[INF2 SUBMIT] cardAnswered:', JSON.stringify(cardAnswered));

      /* Set r99 on visible checkbox questions with no selections */
      var allCBLabels = visibleCBs.concat(inf6Visible ? ['INF6'] : []);
      allCBLabels.forEach(function(q){
        var box = findQ(q);
        if(!box) return;
        var chkd = box.querySelectorAll('input:checked');
        if(chkd.length === 0){
          console.log('[SUBMIT] '+q+' no selections, auto-setting r99');
          syncCheckbox(q, 'r99', true);
        }
      });
      ['INF7','INF8'].forEach(function(q){
        var box = findQ(q);
        if(!box) return;
        var chkd = box.querySelectorAll('input:checked');
        console.log('[INF2 SUBMIT] '+q+' native checked: '+chkd.length);
        if(chkd.length === 0 && cardAnswered[q]){
          console.warn('[INF2 SUBMIT] '+q+' FALLBACK: force-setting by position');
          var allCB = box.querySelectorAll('input[type="checkbox"]');
          if(allCB.length > 0){
            if(noneStates[q]){
              var last = allCB[allCB.length-1];
              last.checked = true; last.click();
              try{ jQuery(last).trigger('click').trigger('change'); }catch(e){}
            } else if(checkStates[q]){
              Object.keys(checkStates[q]).forEach(function(r){
                if(!checkStates[q][r]) return;
                var m3 = r.match(/^r(\d+)$/);
                if(m3){
                  var i3 = parseInt(m3[1],10)-1;
                  if(i3>=0 && i3<allCB.length){
                    allCB[i3].checked = true; allCB[i3].click();
                    try{ jQuery(allCB[i3]).trigger('click').trigger('change'); }catch(e){}
                  }
                }
              });
            }
          }
        }
      });

      var nxt = document.querySelector('.nextPage, .button[type="submit"], input[type="submit"]');
      if(!nxt) nxt = document.querySelector('.button, input.button');
      if(nxt){
        nxt.setAttribute('style','display:block !important;visibility:hidden;position:absolute;left:-9999px;');
        nxt.click();
      } else {
        var form = document.querySelector('form');
        if(form) form.submit();
      }
    });
  }

  /* INIT */
  updateContinue();
})();
</script>
]]></style>

</radio>
<suspend/>


  <note>===================== DEM B-W SCORING =====================</note>

  <text
   label="QMD1"
   size="3"
   where="execute,survey,report">
    <title>Hidden: DEM MaxDiff B-W Matrix</title>
    <exec>
p.sum=[]
groupSize = 4
groups = [p.maxdiff1[i:i+groupSize] for i in range(0, len(p.maxdiff1), groupSize)]
for col in range(0, 10):
    rowsForColumn = groups[col]
    for r in rowsForColumn:
        rowNum = int(r)-1
        QMD1.rows[rowNum][col].val=0
for i in range(0,10):
	best = "QDEM_{}".format(i+1)
	worst = "QDEM_{}".format(i+1)
	best1=allQuestions[best].best.val
	worst1=allQuestions[worst].worst.val
	QMD1.rows[best1][i].val=1
	QMD1.rows[worst1][i].val=-1
for i in QMD1.rows:
	a=0
	for j in range(10):
		if QMD1[i][j].val!=None:
			a1=QMD1[i][j].val
			a+=int(a1)
	QMD1[i].c11.val=a
    </exec>
    <row label="r1">Item-1</row>
    <row label="r2">Item-2</row>
    <row label="r3">Item-3</row>
    <row label="r4">Item-4</row>
    <row label="r5">Item-5</row>
    <row label="r6">Item-6</row>
    <row label="r7">Item-7</row>
    <row label="r8">Item-8</row>
    <row label="r9">Item-9</row>
    <row label="r10">Item-10</row>
    <col label="c1">QDEM_1</col>
    <col label="c2">QDEM_2</col>
    <col label="c3">QDEM_3</col>
    <col label="c4">QDEM_4</col>
    <col label="c5">QDEM_5</col>
    <col label="c6">QDEM_6</col>
    <col label="c7">QDEM_7</col>
    <col label="c8">QDEM_8</col>
    <col label="c9">QDEM_9</col>
    <col label="c10">QDEM_10</col>
    <col label="c11">Row Total</col>
  </text>
  <note>MaxDiff Indices Template --End DEM BW--</note>
  <suspend/>


  <note>===================== DEM Z-SCORING =====================</note>

  <text
   label="XDEM_SEG"
   ss:listDisplay="0"
   where="execute,survey,report">
    <title>HIDDEN: XDEM_SEG (Z-Scores)</title>
    <exec>
DEM_U = [-0.60198, -0.31771, -1.69065, 0.128199, 1.596402, 0.095769, 0.370408, 0.965797, -1.03699, 0.490752]
DEM_S = [1.590145, 1.904432, 1.809285, 1.851639, 1.958779, 1.680348, 1.76441, 1.862097, 2.16036, 1.804034]
DEM_MAX_F = []

for i in range(10):
	XDEM_SEG.rows[i].c1.val=QMD1.rows[i].c11.val
	DEM_MAX_F.append(float(QMD1.rows[i].c11.val))
for i in range(10):
	XDEM_SEG.rows[i].c2.val = DEM_U[i]
for i in range(10):
	XDEM_SEG.rows[i].c3.val = DEM_S[i]
for i in range(10):
	XDEM_SEG.rows[i].c4.val = (DEM_MAX_F[i] - DEM_U[i])/DEM_S[i]

JUSTICE_U = 4.868184
JUSTICE_S = 1.515216
INDUSTRY_U = 5.495869
INDUSTRY_S = 1.315719

justice_raw = float(VECTOR_JUSTICE.r1.val) if VECTOR_JUSTICE.r1.val else 4.0
industry_raw = float(VECTOR_INDUSTRY.r1.val) if VECTOR_INDUSTRY.r1.val else 4.0

XDEM_SEG.r13.c1.val = justice_raw
XDEM_SEG.r13.c2.val = JUSTICE_U
XDEM_SEG.r13.c3.val = JUSTICE_S
XDEM_SEG.r13.c4.val = (justice_raw - JUSTICE_U) / JUSTICE_S

XDEM_SEG.r14.c1.val = industry_raw
XDEM_SEG.r14.c2.val = INDUSTRY_U
XDEM_SEG.r14.c3.val = INDUSTRY_S
XDEM_SEG.r14.c4.val = (industry_raw - INDUSTRY_U) / INDUSTRY_S
    </exec>
    <row label="r1">Tech and data can improve care, but only if we stop bias, protect privacy, and provide benefits fairly.</row>
    <row label="r2">I trust mainstream experts and prefer step-by-step fixes&#x2014;strengthening the ACA, not ripping it up.</row>
    <row label="r3">Digital tools, AI, and start-ups can universalize care faster than big bureaucracies&#x2014;let&#x2019;s remove red tape.</row>
    <row label="r4">Corporate greed hurts ordinary people. Government must crack down on prices, ads, and junk food.</row>
    <row label="r5">Healthcare is a human right. We need Medicare-for-All or something very close to it.</row>
    <row label="r6">Unions fought for good coverage. Policy should protect employer benefits and bargaining power.</row>
    <row label="r7">Racism and inequality drive poor health. Equity must be built into every health decision and budget.</row>
    <row label="r8">Reproductive freedom&#x2014;including abortion access&#x2014;is non-negotiable and central to health policy.</row>
    <row label="r9">My faith calls me to fight for the poor and for broader access&#x2014;especially in under-served communities.</row>
    <row label="r10">Climate change is a public-health emergency. Clean energy and environmental justice are health policy.</row>
    <row label="r13">VECTOR_JUSTICE (ATTITUDE_EQUITY_FUNDING)</row>
    <row label="r14">VECTOR_INDUSTRY (ATTITUDE_INFLUENCE)</row>
    <col label="c1">PUNCH</col>
    <col label="c2">Mean</col>
    <col label="c3">Sigma</col>
    <col label="c4">Z-Score</col>
  </text>
  <suspend/>


  <note>===================== DEM CENTROID MODEL + SOFTMAX =====================</note>

  <exec>
import math

def centroidDistance(resp_answers, segment_coeff):
    return sum((a - c)**2 for a, c in zip(resp_answers, segment_coeff))

def centroidAllSegments(answers, centroid_matrix):
    num_segments = len(centroid_matrix[0])
    d2_list = []
    for c in range(num_segments):
        segment_col = [row[c] for row in centroid_matrix]
        d2_list.append(centroidDistance(answers, segment_col))
    return d2_list

p.dem_z = [float(XDEM_SEG.rows[i].c4.val) for i in range(10)]
p.dem_z.append(float(XDEM_SEG.r13.c4.val))
p.dem_z.append(float(XDEM_SEG.r14.c4.val))

dem_centroids = [
    [-0.5135, -0.1429, -0.3291, 0.544, 0.9293, -0.3204],
    [-0.039, -0.8206, -0.544, 0.1583, 0.6292, 0.8164],
    [-0.4356, -0.3798, -0.3126, 1.1914, 0.6485, -0.4382],
    [0.4891, -0.5913, 0.7544, 0.2422, -0.1966, -0.7195],
    [0.5194, 0.2926, 0.2316, 0.1168, -1.3929, 0.1419],
    [-0.1269, -0.1611, 0.5696, -0.2886, -0.2956, 0.2223],
    [0.257, 0.8708, -0.2005, -0.5704, -0.3712, -0.1654],
    [0.4032, -0.1316, 0.0902, -0.9386, -0.1134, 0.547],
    [-0.7075, 1.0908, -0.1312, 0.2406, 0.0411, -0.5795],
    [0.1626, -0.2097, -0.1184, -0.7095, 0.2866, 0.5285],
    [0.7218, 0.4355, -0.4757, -0.3332, -0.4753, 0.2212],
    [0.7961, 0.1727, 0.0062, -0.267, -0.5004, -0.0547]
]

p.dem_d2 = centroidAllSegments(p.dem_z, dem_centroids)

p.dem_exp = [math.exp(-d/2.0) for d in p.dem_d2]
dem_exp_sum = sum(p.dem_exp)
p.dem_prob = [(e/dem_exp_sum)*100.0 for e in p.dem_exp]

for i in range(6):
    XDEM_SOFTMAX.rows[i].c1.val = round(p.dem_d2[i], 4)
    XDEM_SOFTMAX.rows[i].c2.val = round(p.dem_exp[i], 6)
    XDEM_SOFTMAX.rows[i].c3.val = round(p.dem_prob[i], 2)

sorted_probs = sorted(enumerate(p.dem_prob), key=lambda x: -x[1])
XDEM_SEG_FINAL_1.val = XDEM_SEG_FINAL_1.rows[sorted_probs[0][0]].index
XDEM_SEG_FINAL_2.val = XDEM_SEG_FINAL_2.rows[sorted_probs[1][0]].index

gap = sorted_probs[0][1] - sorted_probs[1][1]
XDEM_CONFIDENCE.r1.val = round(sorted_probs[0][1], 2)
XDEM_CONFIDENCE.r2.val = round(sorted_probs[1][1], 2)
XDEM_CONFIDENCE.r3.val = round(gap, 2)
entropy_val = -sum((pr/100.0)*math.log(pr/100.0+1e-12) for pr in p.dem_prob)
XDEM_CONFIDENCE.r4.val = round(entropy_val, 4)
  </exec>

  <text
   label="XDEM_SOFTMAX"
   randomize="0"
   size="40"
   where="execute,survey,report">
    <title>HIDDEN: DEM Softmax Probabilities</title>
    <row label="r1">11. Universal Care Progressives (UCP)</row>
    <row label="r2">12. Faith and Justice Progressives (FJP)</row>
    <row label="r3">13. Health Care Protectionists (HCP)</row>
    <row label="r4">14. Health Abundance Democrats (HAD)</row>
    <row label="r5">15. Health Care Incrementalists (HCI)</row>
    <row label="r6">16. Global Health Institutionalists (GHI)</row>
    <col label="c1">D-Squared</col>
    <col label="c2">exp(-D2/2)</col>
    <col label="c3">Probability %</col>
  </text>
  <suspend/>

  <radio
   label="XDEM_SEG_FINAL_1"
   optional="1"
   randomize="0"
   where="execute,survey,report">
    <title>CENTROID+SOFTMAX - DEM SEGMENTS - First Maximum</title>
    <row label="r1">11. Universal Care Progressives (UCP)</row>
    <row label="r2">12. Faith and Justice Progressives (FJP)</row>
    <row label="r3">13. Health Care Protectionists (HCP)</row>
    <row label="r4">14. Health Abundance Democrats (HAD)</row>
    <row label="r5">15. Health Care Incrementalists (HCI)</row>
    <row label="r6">16. Global Health Institutionalists (GHI)</row>
  </radio>
  <suspend/>

  <radio
   label="XDEM_SEG_FINAL_2"
   optional="1"
   randomize="0"
   where="execute,survey,report">
    <title>CENTROID+SOFTMAX - DEM SEGMENTS - Second Maximum</title>
    <row label="r1">11. Universal Care Progressives (UCP)</row>
    <row label="r2">12. Faith and Justice Progressives (FJP)</row>
    <row label="r3">13. Health Care Protectionists (HCP)</row>
    <row label="r4">14. Health Abundance Democrats (HAD)</row>
    <row label="r5">15. Health Care Incrementalists (HCI)</row>
    <row label="r6">16. Global Health Institutionalists (GHI)</row>
  </radio>
  <suspend/>

  <text
   label="XDEM_CONFIDENCE"
   size="20"
   where="execute,survey,report">
    <title>HIDDEN: DEM Confidence Metrics</title>
    <row label="r1">Top Segment Probability %</row>
    <row label="r2">Second Segment Probability %</row>
    <row label="r3">Probability Gap (P1-P2)</row>
    <row label="r4">Entropy</row>
  </text>
  <suspend/>

  <note>MaxDiff Indices Template --End DEM--</note>
</block>
<suspend/>


<note>===================== PAGE GROUP 3 -- GOP MaxDiff Block =====================</note>

<block label="b12" cond="TYPING_MODULE.r1 or TYPING_MODULE.r3">
  <note>MaxDiff Indices Template --Start GOP--</note>
  <exec when="init">
QMD3_md = setupMaxDiffFile("GOPDesign.dat")
  </exec>
  <suspend/>
  <quota label="quo2" overquota="noqual" sheet="GOP Quota"/>
  <suspend/>
  <number
   label="QMD3_Version"
   size="3"
   verify="range(1,100)"
   where="execute,survey,report">
    <title>QMD3 - MaxDiff Version</title>
    <exec>
for x in p.markers:
    if "/GOP Quota/GOP_" in x:
        QMD3_Version.val = int(x.split("_")[-1])
        break
    </exec>
  </number>
  <suspend/>
  <exec>
p.startTime = timeSpent()
  </exec>

  <html label="QMD3_intro"><![CDATA[
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style type="text/css">
  body, .survey-body, .main-content { background: #F5F2ED !important; }
  .nextPage, .button {
    background: #333 !important; color: #fff !important; border: none !important;
    border-radius: 24px !important; padding: 14px 40px !important;
    font-family: Inter,sans-serif !important; font-size: 14px !important;
    font-weight: 700 !important; letter-spacing: .08em !important;
    text-transform: uppercase !important; cursor: pointer !important;
    max-width: 320px !important; margin: 0 auto !important;
    display: block !important;
  }
</style>
<div style="max-width:480px;margin:0 auto;padding:0 16px 16px;text-align:center;">
  <div style="padding:4px 0 36px;">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM" style="height:48px;width:auto;">
  </div>
  <div style="background:#fff;border:1px solid #E0DCD6;border-radius:12px;padding:28px 24px 24px;margin-bottom:24px;">
    <p style="font-family:Fraunces,Georgia,serif;font-size:15px;color:#333;line-height:1.55;margin:0 0 16px;">
      In this section we'll ask about a mix of political and social issues. Some questions may feel personal, and some statements you may agree with strongly and others not so much. Always just go with your first, honest reaction.
    </p>
    <p style="font-family:Fraunces,Georgia,serif;font-size:15px;color:#333;line-height:1.55;margin:0 0 16px;">
      You'll see a series of cards, each with four statements. On each card, choose the one that sounds <strong>most</strong> like you and the one that sounds <strong>least</strong> like you.
    </p>
    <p style="font-family:Fraunces,Georgia,serif;font-size:15px;color:#333;line-height:1.55;margin:0;">
      Some statements may show up again&#x2014;that's intentional. There are no right or wrong answers. Go with the statement that feels most true&#x2014;and most familiar&#x2014;to you.
    </p>
  </div>
</div>
]]></html>
  <suspend/>

  <note>GOP MaxDiff Loop: 12 items, 4 per task, 12 tasks</note>
  <loop label="QMD3_md_loop" vars="task">
    <title>QMD3 - GOP MaxDiff Loop</title>
    <block label="QMD3_md_block" randomize="1">
      <radio
     label="QGOP_[loopvar: task]"
     adim="cols"
     grouping="cols"
     shuffle="rows"
     ss:listDisplay="0"
     ss:questionClassNames="QMD3_maxdiff"
     unique="1">
        <title>Select the one statement that sounds MOST like your own point of view (on the left), and the statement that sounds LEAST like you (on the right)</title>
        <exec>
setupMaxDiffItemsI( QMD3_md, "v%d_t%d" % (QMD3_Version.val, [loopvar: task]), QGOP_[loopvar: task])
p.MDcount = str(QMD3_md_loop_expanded.order.index([loopvar: task]-1)+1)
        </exec>
        <col label="best"><strong>sounds MOST like me</strong></col>
        <col label="worst"><strong>sounds LEAST like me</strong></col>
        <row label="item1">Government should stay small but defend marriage, family life, and personal responsibility.</row>
        <row label="item2">Big pharma and big agribusiness can&#x2019;t be trusted; local food and local control are healthier.</row>
        <row label="item3">Drug and hospital prices are crushing people. I don&#x2019;t care who fixes it&#x2014;just bring costs down.</row>
        <row label="item4">Small-town hospitals keep closing. Government must improve access to care for rural America.</row>
        <row label="item5">Vaccines are unsafe, full stop. No one should be forced or pressured to take them.</row>
        <row label="item6">My body, my choice&#x2014;whether mandates, supplements, or experimental drugs. Government should butt out.</row>
        <row label="item7">Modern medicine ignores the whole person. Natural and alternative therapies deserve equal respect and coverage.</row>
        <row label="item8">Clean, whole-food living&#x2014;not pills&#x2014;keeps us healthy. The system ignores it because there&#x2019;s no profit in it.</row>
        <row label="item9">Human beings thrive on meat-heavy, low-carb diets. Government guidelines are junk science.</row>
        <row label="item10">We must protect U.S. patients, workers, and supply chains first. Foreign competition and immigration come second.</row>
        <row label="item11">Free markets work best, but I defer to FDA and CDC experts on safety and innovation.</row>
        <row label="item12">Competition&#x2014;private insurance, transparent prices, fewer rules&#x2014;is the key to cheaper, better care.</row>
        <style mode="before" name="question.header"><![CDATA[
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,400&amp;family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet">
<style type="text/css">
    body, .survey-body, .main-content { background: #F5F2ED !important; }
    .QMD3_maxdiff .question-text { display: none !important; }
    .QMD3_maxdiff .comment { display: none !important; }
    .nextPage, .button { display: none !important; }
    /* hide native rendering until JS rebuilds it */
    .QMD3_maxdiff .survey-questions-container,
    .QMD3_maxdiff table,
    .QMD3_maxdiff .col-legend-group,
    .QMD3_maxdiff .grouping-box { display: none !important; }
    .QMD3_maxdiff .md-custom-ui { display: block !important; }

    /* --- custom MaxDiff UI --- */
    .md-custom-ui { max-width: 640px; margin: 0 auto; }
    .md-header-row { display: flex; justify-content: space-between; padding: 0 8px 8px; }
    .md-header-row span { font-family: Inter,sans-serif; font-size: 10px; font-weight: 800;
        letter-spacing: .06em; text-transform: uppercase; }
    .md-header-most { color: #6B7F4E; }
    .md-header-least { color: #C62828; }
    .md-row { display: flex; align-items: center; padding: 12px 4px;
        border-bottom: 1px solid #EDE9E3; gap: 10px; }
    .md-row:last-child { border-bottom: none; }
    .md-row-text { flex: 1; font-family: Fraunces,Georgia,serif; font-size: 16px;
        color: #333; line-height: 1.5; text-align: center; padding: 0 8px; }
    .md-row:hover { background: rgba(0,0,0,.03); border-radius: 8px; }
    .md-row.md-has-best { background: rgba(107,127,78,.08); border-radius: 8px; }
    .md-row.md-has-worst { background: rgba(198,40,40,.06); border-radius: 8px; }

    /* spherical radio buttons */
    .md-radio { -webkit-appearance: none; -moz-appearance: none; appearance: none;
        width: 44px; height: 44px; min-width: 44px; border-radius: 50%;
        border: 2px solid #C5C0B8;
        background: radial-gradient(circle at 35% 35%, #FAFAFA, #E8E4DE);
        box-shadow: 0 2px 6px rgba(0,0,0,.12), inset 0 1px 2px rgba(255,255,255,.6);
        cursor: pointer; transition: all .2s ease; outline: none; }
    .md-radio:hover { border-color: #999;
        box-shadow: 0 3px 10px rgba(0,0,0,.18), inset 0 1px 2px rgba(255,255,255,.6);
        transform: scale(1.08); }
    .md-radio.md-best-checked {
        background: radial-gradient(circle at 35% 35%, #8CAF6B, #5A7E3A);
        border-color: #4A6E2A;
        box-shadow: 0 3px 10px rgba(90,126,58,.4), inset 0 1px 3px rgba(255,255,255,.3); }
    .md-radio.md-worst-checked {
        background: radial-gradient(circle at 35% 35%, #E05555, #C62828);
        border-color: #A01E1E;
        box-shadow: 0 3px 10px rgba(198,40,40,.4), inset 0 1px 3px rgba(255,255,255,.3); }
    .md-progress-wrap { padding: 0 0 8px; }
    .md-section-header { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 6px; }
    .md-section-title { font-family: Inter,sans-serif; font-size: 9px; font-weight: 700;
        letter-spacing: .06em; text-transform: uppercase; color: #888;
        text-decoration: underline; text-underline-offset: 2px; }
    .md-progress-label { font-family: Inter,sans-serif; font-size: 9px; font-weight: 600;
        letter-spacing: .05em; text-transform: uppercase; color: #888; }
    .md-progress-track { height: 5px; background: #E0E0E0; border-radius: 3px; }
    .md-progress-fill { height: 100%; width: 0%; background: #6B7F4E; border-radius: 3px;
        transition: width .45s cubic-bezier(.4,0,.2,1); }
    .md-card-counter { font-family: Inter,sans-serif; font-size: 9px; font-weight: 600;
        letter-spacing: .08em; text-transform: uppercase; color: #888;
        margin-bottom: 4px; text-align: right; }
</style>
<div style="max-width:640px;margin:0 auto;padding:0 16px 16px;">
  <div style="text-align:center;padding:4px 0 36px;">
    <img src="[rel PRISM_glyph.svg]" alt="PRISM" style="height:40px;width:auto;">
  </div>
  <div class="md-progress-wrap">
    <div class="md-section-header">
      <span class="md-section-title">SECTION 2: VALUES &amp; IDENTITY</span>
      <span class="md-progress-label">${p.MDcount} of ${len(QMD3_md_loop_expanded.order)} Questions</span>
    </div>
    <div class="md-progress-track"><div class="md-progress-fill" id="md-progress-fill-gop"></div></div>
  </div>
  <div class="md-card-counter">Card ${p.MDcount} of ${len(QMD3_md_loop_expanded.order)}</div>
  <div style="background:#fff;border:1px solid #E0DCD6;border-radius:10px;padding:22px 24px 20px;margin-bottom:20px;">
    <div style="font-family:Fraunces,Georgia,serif;font-size:17px;font-weight:700;color:#333;line-height:1.42;margin-bottom:8px;">
      From the set of short statements expressing different points of view about politics, culture and health.
    </div>
    <div style="font-size:13px;color:#555;text-align:center;margin-bottom:14px;line-height:1.5;">
      <span style="color:#6B7F4E;font-weight:700;">&#x2B05;</span>
      Select one statement that sounds MOST like your own point of view, and
      the one statement that sounds LEAST like you
      <span style="color:#C62828;font-weight:700;">&#x27A1;</span>
    </div>
]]></style>
        <style name="question.top-legend"><![CDATA[
\@if ec.simpleList
    $(legends)
\@else
    <$(tag) class="maxdiff-header-legend row row-col-legends row-col-legends-top ${"mobile-top-row-legend " if mobileOnly else ""}${"GtTenColumns " if ec.colCount > 10 else ""}colCount-$(colCount)">
        ${"%s%s" % (legends.split("</th>")[0],"</th>")}
       $(left)
        ${"%s%s" % (legends.split("</th>")[1],"</th>")}
    </$(tag)>
    \@if not simple
  </tbody>
  <tbody>
    \@endif
\@endif
]]></style>
        <style name="question.row"><![CDATA[
\@if ec.simpleList
    $(elements)
\@else
    <$(tag) class="maxdiff-row row row-elements $(style) colCount-$(colCount)">
        ${"%s%s" % (elements.split("</td>")[0],"</td>")}
        $(left)
        ${"%s%s" % (elements.split("</td>")[1],"</td>")}
    </$(tag)>
\@endif
]]></style>
        <style mode="after" name="question.footer"><![CDATA[
  </div>
  <div style="text-align:center;padding:24px 0 16px;">
    <button type="button" id="md-continue-btn"
            style="background:#6B7F4E;color:#fff;border:none;border-radius:24px;padding:14px 36px;font-family:Inter,sans-serif;font-size:14px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;cursor:pointer;opacity:.4;pointer-events:none;">
      CONTINUE &gt;
    </button>
  </div>
  <div style="position:fixed;bottom:0;left:0;right:0;z-index:100;background:#F5F2ED;border-top:1px solid #E0DCD6;padding:10px 20px;text-align:center;font-size:10px;color:#AAA;font-family:Inter,sans-serif;line-height:1.4;">
    Your responses are held strictly confidential.
    See our <a href="#" style="color:#6B7F4E;text-decoration:none;">Privacy Policy</a>
    and commitment to research ethics and integrity.
  </div>
</div>
<script>
(function(){
  /* set progress bar width from Decipher template vars */
  var pFill = document.getElementById('md-progress-fill-gop');
  if(pFill){
    var curCard = parseInt('${p.MDcount}',10) || 1;
    var totalCards = parseInt('${len(QMD3_md_loop_expanded.order)}',10) || 1;
    pFill.style.width = Math.round((curCard/totalCards)*100)+'%';
  }

  var q = document.querySelector('.QMD3_maxdiff');
  if(!q) return;

  /* find all native radio inputs grouped by column (best/worst) */
  var bestRadios = [].slice.call(q.querySelectorAll('input[type="radio"][name*="_best_"],input[type="radio"][name*=".best."],input[type="radio"][name*="c1"]'));
  var worstRadios = [].slice.call(q.querySelectorAll('input[type="radio"][name*="_worst_"],input[type="radio"][name*=".worst."],input[type="radio"][name*="c2"]'));

  /* fallback: if can't find by name pattern, find by grouping structure */
  if(!bestRadios.length || !worstRadios.length){
    var allRadios = [].slice.call(q.querySelectorAll('input[type="radio"]'));
    /* group by name attribute */
    var groups = {};
    allRadios.forEach(function(r){ (groups[r.name] = groups[r.name]||[]).push(r); });
    var gnames = Object.keys(groups);
    console.log('[MD-GOP] radio groups found:', gnames);
    if(gnames.length >= 2){
      /* first group = best, second = worst */
      bestRadios = groups[gnames[0]];
      worstRadios = groups[gnames[1]];
    }
  }

  if(!bestRadios.length || !worstRadios.length){
    console.warn('[MD-GOP] could not find best/worst radios');
    return;
  }

  console.log('[MD-GOP] best:', bestRadios.length, 'worst:', worstRadios.length);

  /* extract item labels from the native rendering */
  var items = [];
  bestRadios.forEach(function(r,i){
    var row = r.closest('tr, .row-element, .row, label, li');
    var text = '';
    if(row){
      var legend = row.querySelector('th.row-legend, td.row-legend, .row-legend-text, .row-legend');
      if(!legend) legend = row.querySelector('.text');
      text = legend ? legend.textContent.trim() : '';
      if(!text){
        var clone = row.cloneNode(true);
        clone.querySelectorAll('input,th.col-legend,td.col-legend').forEach(function(el){ el.remove(); });
        text = clone.textContent.trim();
      }
    }
    if(!text) text = 'Statement '+(i+1);
    /* strip any [label] prefix from Decipher row text */
    var bi = text.indexOf(']');
    if(bi >= 0 && bi < 30) text = text.substring(bi+1).trim();
    console.log('[MD-GOP] item'+i+' text:', JSON.stringify(text));
    items.push(text);
  });

  /* build custom UI */
  var wrap = document.createElement('div');
  wrap.className = 'md-custom-ui';
  wrap.innerHTML = '<div class="md-header-row">' +
    '<span class="md-header-most">MOST<br>LIKE ME</span>' +
    '<span class="md-header-least">LEAST<br>LIKE ME</span></div>';

  var bestSel = -1, worstSel = -1;

  function updateUI(){
    var rows = wrap.querySelectorAll('.md-row');
    var allBest = wrap.querySelectorAll('.md-radio-best');
    var allWorst = wrap.querySelectorAll('.md-radio-worst');
    for(var i=0;i<rows.length;i++){
      rows[i].classList.remove('md-has-best','md-has-worst');
      allBest[i].classList.remove('md-best-checked');
      allWorst[i].classList.remove('md-worst-checked');
      if(i===bestSel){ rows[i].classList.add('md-has-best'); allBest[i].classList.add('md-best-checked'); }
      if(i===worstSel){ rows[i].classList.add('md-has-worst'); allWorst[i].classList.add('md-worst-checked'); }
    }
    /* enable continue only when both selected */
    var btn = document.getElementById('md-continue-btn');
    if(btn){
      if(bestSel>=0 && worstSel>=0){ btn.style.opacity='1'; btn.style.pointerEvents='auto'; }
      else { btn.style.opacity='.4'; btn.style.pointerEvents='none'; }
    }
  }

  items.forEach(function(txt,i){
    var row = document.createElement('div');
    row.className = 'md-row';
    row.innerHTML = '<button type="button" class="md-radio md-radio-best" data-idx="'+i+'"></button>' +
      '<div class="md-row-text">'+txt+'</div>' +
      '<button type="button" class="md-radio md-radio-worst" data-idx="'+i+'"></button>';
    wrap.appendChild(row);
  });

  /* click handlers */
  wrap.addEventListener('click', function(e){
    var btn = e.target.closest('.md-radio');
    if(!btn) return;
    var idx = parseInt(btn.getAttribute('data-idx'),10);
    if(btn.classList.contains('md-radio-best')){
      if(idx===worstSel) return; /* can't pick same row */
      bestSel = (bestSel===idx) ? -1 : idx;
      /* sync to native radio */
      if(bestSel>=0 && bestRadios[bestSel]){ bestRadios[bestSel].checked=true; bestRadios[bestSel].click(); }
    } else {
      if(idx===bestSel) return;
      worstSel = (worstSel===idx) ? -1 : idx;
      if(worstSel>=0 && worstRadios[worstSel]){ worstRadios[worstSel].checked=true; worstRadios[worstSel].click(); }
    }
    updateUI();
  });

  /* insert custom UI before the native question */
  q.insertBefore(wrap, q.firstChild);

  /* continue button */
  var btn = document.getElementById('md-continue-btn');
  if(btn){
    btn.addEventListener('click', function(){
      if(bestSel<0||worstSel<0) return;
      var nxt = document.querySelector('.nextPage, .button[type="submit"], input[type="submit"]');
      if(!nxt) nxt = document.querySelector('.button, input.button');
      if(nxt){
        nxt.setAttribute('style','display:block !important;visibility:hidden;position:absolute;left:-9999px;');
        nxt.click();
      } else {
        var form = document.querySelector('form');
        if(form) form.submit();
      }
    });
  }
})();
</script>
]]></style>
      </radio>
      <suspend/>
    </block>
    <looprow label="1"><loopvar name="task">1</loopvar></looprow>
    <looprow label="2"><loopvar name="task">2</loopvar></looprow>
    <looprow label="3"><loopvar name="task">3</loopvar></looprow>
    <looprow label="4"><loopvar name="task">4</loopvar></looprow>
    <looprow label="5"><loopvar name="task">5</loopvar></looprow>
    <looprow label="6"><loopvar name="task">6</loopvar></looprow>
    <looprow label="7"><loopvar name="task">7</loopvar></looprow>
    <looprow label="8"><loopvar name="task">8</loopvar></looprow>
    <looprow label="9"><loopvar name="task">9</loopvar></looprow>
    <looprow label="10"><loopvar name="task">10</loopvar></looprow>
    <looprow label="11"><loopvar name="task">11</loopvar></looprow>
    <looprow label="12"><loopvar name="task">12</loopvar></looprow>
  </loop>

  <float
   label="QMD3_Timer"
   size="15"
   where="execute,survey,report">
    <title>QMD3 - GOP MaxDiff Timer (Minutes)</title>
    <exec>
QMD3_Timer.val = (timeSpent() - p.startTime) / 60.0
    </exec>
  </float>
  <note>MaxDiff Indices Template --End GOP Loop--</note>
  <suspend/>


  <note>===================== GOP B-W SCORING =====================</note>

  <text
   label="QMD3"
   size="3"
   where="execute,survey,report">
    <title>Hidden: GOP MaxDiff B-W Matrix</title>
    <exec>
p.sum=[]
groupSize = 4
groups = [p.maxdiff1[i:i+groupSize] for i in range(0, len(p.maxdiff1), groupSize)]
for col in range(0, 12):
    rowsForColumn = groups[col]
    for r in rowsForColumn:
        rowNum = int(r)-1
        QMD3.rows[rowNum][col].val=0
for i in range(0,12):
	best = "QGOP_{}".format(i+1)
	worst = "QGOP_{}".format(i+1)
	best1=allQuestions[best].best.val
	worst1=allQuestions[worst].worst.val
	QMD3.rows[best1][i].val=1
	QMD3.rows[worst1][i].val=-1
for i in QMD3.rows:
	a=0
	for j in range(12):
		if QMD3[i][j].val!=None:
			a1=QMD3[i][j].val
			a+=int(a1)
	QMD3[i].c13.val=a
    </exec>
    <row label="r1">Government should stay small but defend marriage, family life, and personal responsibility.</row>
    <row label="r2">Big pharma and big agribusiness can&#x2019;t be trusted; local food and local control are healthier.</row>
    <row label="r3">Drug and hospital prices are crushing people. I don&#x2019;t care who fixes it&#x2014;just bring costs down.</row>
    <row label="r4">Small-town hospitals keep closing. Government must improve access to care for rural America.</row>
    <row label="r5">Vaccines are unsafe, full stop. No one should be forced or pressured to take them.</row>
    <row label="r6">My body, my choice&#x2014;whether mandates, supplements, or experimental drugs. Government should butt out.</row>
    <row label="r7">Modern medicine ignores the whole person. Natural and alternative therapies deserve equal respect and coverage.</row>
    <row label="r8">Clean, whole-food living&#x2014;not pills&#x2014;keeps us healthy. The system ignores it because there&#x2019;s no profit in it.</row>
    <row label="r9">Human beings thrive on meat-heavy, low-carb diets. Government guidelines are junk science.</row>
    <row label="r10">We must protect U.S. patients, workers, and supply chains first. Foreign competition and immigration come second.</row>
    <row label="r11">Free markets work best, but I defer to FDA and CDC experts on safety and innovation.</row>
    <row label="r12">Competition&#x2014;private insurance, transparent prices, fewer rules&#x2014;is the key to cheaper, better care.</row>
    <col label="c1">QGOP_1</col>
    <col label="c2">QGOP_2</col>
    <col label="c3">QGOP_3</col>
    <col label="c4">QGOP_4</col>
    <col label="c5">QGOP_5</col>
    <col label="c6">QGOP_6</col>
    <col label="c7">QGOP_7</col>
    <col label="c8">QGOP_8</col>
    <col label="c9">QGOP_9</col>
    <col label="c10">QGOP_10</col>
    <col label="c11">QGOP_11</col>
    <col label="c12">QGOP_12</col>
    <col label="c13">Row Total</col>
  </text>
  <suspend/>


  <note>===================== GOP Z-SCORING =====================</note>

  <text
   label="XGOP_SEG"
   size="25"
   where="execute,survey,report">
    <title>HIDDEN: XGOP_SEG (Z-Scores)</title>
    <exec>
GOP_U = [0.22251, 0.034617, 0.476305, -0.07664, -0.98778, 0.026842, -0.37542, 0.619956, -0.85117, 0.776564, -0.39097, 0.525176]
GOP_S = [1.788406, 1.540211, 1.862495, 1.702904, 2.127103, 1.901242, 1.907928, 1.787279, 1.775616, 1.828781, 1.902662, 1.797109]
GOP_MAX_F = []

for i in XGOP_SEG.rows:
	i.c1.val=QMD3[i].c13.val
	GOP_MAX_F.append(float(QMD3[i].c13.val))
for i in range(12):
	XGOP_SEG.rows[i].c2.val = GOP_U[i]
for i in range(12):
	XGOP_SEG.rows[i].c3.val = GOP_S[i]
for i in range(12):
	XGOP_SEG.rows[i].c4.val = (GOP_MAX_F[i] - GOP_U[i])/GOP_S[i]
    </exec>
    <row label="r1">Government should stay small but defend marriage, family life, and personal responsibility.</row>
    <row label="r2">Big pharma and big agribusiness can&#x2019;t be trusted; local food and local control are healthier.</row>
    <row label="r3">Drug and hospital prices are crushing people. I don&#x2019;t care who fixes it&#x2014;just bring costs down.</row>
    <row label="r4">Small-town hospitals keep closing. Government must improve access to care for rural America.</row>
    <row label="r5">Vaccines are unsafe, full stop. No one should be forced or pressured to take them.</row>
    <row label="r6">My body, my choice&#x2014;whether mandates, supplements, or experimental drugs. Government should butt out.</row>
    <row label="r7">Modern medicine ignores the whole person. Natural and alternative therapies deserve equal respect and coverage.</row>
    <row label="r8">Clean, whole-food living&#x2014;not pills&#x2014;keeps us healthy. The system ignores it because there&#x2019;s no profit in it.</row>
    <row label="r9">Human beings thrive on meat-heavy, low-carb diets. Government guidelines are junk science.</row>
    <row label="r10">We must protect U.S. patients, workers, and supply chains first. Foreign competition and immigration come second.</row>
    <row label="r11">Free markets work best, but I defer to FDA and CDC experts on safety and innovation.</row>
    <row label="r12">Competition&#x2014;private insurance, transparent prices, fewer rules&#x2014;is the key to cheaper, better care.</row>
    <col label="c1">PUNCH</col>
    <col label="c2">Mean</col>
    <col label="c3">Sigma</col>
    <col label="c4">Z-Score</col>
  </text>
  <suspend/>


  <note>===================== GOP CENTROID MODEL + SOFTMAX =====================</note>

  <exec>
import math

def centroidDistance(resp_answers, segment_coeff):
    return sum((a - c)**2 for a, c in zip(resp_answers, segment_coeff))

def centroidAllSegments(answers, centroid_matrix):
    num_segments = len(centroid_matrix[0])
    d2_list = []
    for c in range(num_segments):
        segment_col = [row[c] for row in centroid_matrix]
        d2_list.append(centroidDistance(answers, segment_col))
    return d2_list

p.gop_z = [float(x) for x in XGOP_SEG.c4.values]

gop_centroids = [
    [0.2, 0.0096, 0.9146, -0.1336, -0.6956, 0.5846, 0.4648, -0.2554, -0.739, -0.8787],
    [-0.0608, 0.0308, -0.5647, 0.0053, 0.7257, -0.0362, 0.0185, -0.4401, 0.1704, 0.6758],
    [-0.0633, 0.67, -0.4791, -0.3529, 0.712, -0.2058, -0.5104, -0.4564, 0.3953, 0.0561],
    [0.8912, 0.4863, -0.2283, 0.1686, 0.9888, -0.7418, -0.9072, 0.1943, -0.1989, -0.4581],
    [0.1561, -0.7521, -0.6867, 0.6036, -0.1513, -0.0754, 0.8258, 0.3333, -0.0793, 1.0337],
    [-0.6021, 0.1277, -0.2994, 0.0551, 0.2664, -0.4837, -0.1493, 0.1478, 0.5757, 0.4379],
    [-0.1778, -0.9908, -0.8135, 0.233, -0.1464, 0.0851, 0.236, 0.9657, 0.3877, 0.9297],
    [-0.7705, -0.2343, -0.3477, -1.0279, -0.572, 0.6932, 0.1564, 0.2632, 0.5845, 0.5142],
    [0.1964, -0.5763, 0.0446, 1.363, -0.0369, -0.4206, 0.9004, 0.2931, -0.5885, 0.1112],
    [-0.3176, 0.7097, 0.4794, -0.8491, -0.2762, 0.4546, -0.4476, -0.8232, 0.3467, -0.4787],
    [0.5922, 0.291, 1.1052, 0.6792, 0.0203, -0.5188, -0.4748, 0.0904, -0.7155, -1.192],
    [-0.0166, 0.3879, 0.9236, -0.8652, -0.687, 0.6888, -0.2467, -0.4652, -0.1536, -0.8748]
]

p.gop_d2 = centroidAllSegments(p.gop_z, gop_centroids)

p.gop_exp = [math.exp(-d/2.0) for d in p.gop_d2]
gop_exp_sum = sum(p.gop_exp)
p.gop_prob = [(e/gop_exp_sum)*100.0 for e in p.gop_exp]

for i in range(10):
    XGOP_SOFTMAX.rows[i].c1.val = round(p.gop_d2[i], 4)
    XGOP_SOFTMAX.rows[i].c2.val = round(p.gop_exp[i], 6)
    XGOP_SOFTMAX.rows[i].c3.val = round(p.gop_prob[i], 2)

sorted_probs = sorted(enumerate(p.gop_prob), key=lambda x: -x[1])
XGOP_SEG_FINAL_1.val = XGOP_SEG_FINAL_1.rows[sorted_probs[0][0]].index
XGOP_SEG_FINAL_2.val = XGOP_SEG_FINAL_2.rows[sorted_probs[1][0]].index

gap = sorted_probs[0][1] - sorted_probs[1][1]
XGOP_CONFIDENCE.r1.val = round(sorted_probs[0][1], 2)
XGOP_CONFIDENCE.r2.val = round(sorted_probs[1][1], 2)
XGOP_CONFIDENCE.r3.val = round(gap, 2)
entropy_val = -sum((pr/100.0)*math.log(pr/100.0+1e-12) for pr in p.gop_prob)
XGOP_CONFIDENCE.r4.val = round(entropy_val, 4)
  </exec>

  <text
   label="XGOP_SOFTMAX"
   randomize="0"
   size="40"
   where="execute,survey,report">
    <title>HIDDEN: GOP Softmax Probabilities</title>
    <row label="r1">1. Trust-the-Science Pragmatists (TSP)</row>
    <row label="r2">2. Consumer Empowerment Champions (CEC)</row>
    <row label="r3">3. Traditional Conservatives (TC)</row>
    <row label="r4">4. Health Futurist (HF)</row>
    <row label="r5">5. Price Populists (PP)</row>
    <row label="r6">6. Wellness Evangelists (WE)</row>
    <row label="r7">7. Paleo Freedom Fighters (PFF)</row>
    <row label="r8">8. Holistic Health Naturalists (HHN)</row>
    <row label="r9">9. Medical Freedom Libertarians (MFL)</row>
    <row label="r10">10. Vaccine Skeptics (VS)</row>
    <col label="c1">D-Squared</col>
    <col label="c2">exp(-D2/2)</col>
    <col label="c3">Probability %</col>
  </text>
  <suspend/>

  <radio
   label="XGOP_SEG_FINAL_1"
   optional="1"
   randomize="0"
   where="execute,survey,report">
    <title>CENTROID+SOFTMAX - GOP SEGMENTS - First Maximum</title>
    <row label="r1">1. Trust-the-Science Pragmatists (TSP)</row>
    <row label="r2">2. Consumer Empowerment Champions (CEC)</row>
    <row label="r3">3. Traditional Conservatives (TC)</row>
    <row label="r4">4. Health Futurist (HF)</row>
    <row label="r5">5. Price Populists (PP)</row>
    <row label="r6">6. Wellness Evangelists (WE)</row>
    <row label="r7">7. Paleo Freedom Fighters (PFF)</row>
    <row label="r8">8. Holistic Health Naturalists (HHN)</row>
    <row label="r9">9. Medical Freedom Libertarians (MFL)</row>
    <row label="r10">10. Vaccine Skeptics (VS)</row>
  </radio>
  <suspend/>

  <radio
   label="XGOP_SEG_FINAL_2"
   optional="1"
   randomize="0"
   where="execute,survey,report">
    <title>CENTROID+SOFTMAX - GOP SEGMENTS - Second Maximum</title>
    <row label="r1">1. Trust-the-Science Pragmatists (TSP)</row>
    <row label="r2">2. Consumer Empowerment Champions (CEC)</row>
    <row label="r3">3. Traditional Conservatives (TC)</row>
    <row label="r4">4. Health Futurist (HF)</row>
    <row label="r5">5. Price Populists (PP)</row>
    <row label="r6">6. Wellness Evangelists (WE)</row>
    <row label="r7">7. Paleo Freedom Fighters (PFF)</row>
    <row label="r8">8. Holistic Health Naturalists (HHN)</row>
    <row label="r9">9. Medical Freedom Libertarians (MFL)</row>
    <row label="r10">10. Vaccine Skeptics (VS)</row>
  </radio>
  <suspend/>

  <text
   label="XGOP_CONFIDENCE"
   size="20"
   where="execute,survey,report">
    <title>HIDDEN: GOP Confidence Metrics</title>
    <row label="r1">Top Segment Probability %</row>
    <row label="r2">Second Segment Probability %</row>
    <row label="r3">Probability Gap (P1-P2)</row>
    <row label="r4">Entropy</row>
  </text>
  <suspend/>

  <note>MaxDiff Indices Template --End GOP--</note>
</block>
<suspend/>


<note>===================== FINAL SEGMENT ASSIGNMENT =====================</note>

<radio
  label="XSEG_ASSIGNED"
  optional="1"
  where="execute,survey,report">
  <title>Final Segment Assignment (1-16)</title>
  <comment>GOP Segments (1-10), DEM Segments (11-16)</comment>
  <exec>
if XGOP_SEG_FINAL_1.any:
    XSEG_ASSIGNED.val = XGOP_SEG_FINAL_1.val
elif XDEM_SEG_FINAL_1.any:
    XSEG_ASSIGNED.val = XDEM_SEG_FINAL_1.val + 10
  </exec>
  <row label="r1">1. Trust-the-Science Pragmatists (TSP)</row>
  <row label="r2">2. Consumer Empowerment Champions (CEC)</row>
  <row label="r3">3. Traditional Conservatives (TC)</row>
  <row label="r4">4. Health Futurist (HF)</row>
  <row label="r5">5. Price Populists (PP)</row>
  <row label="r6">6. Wellness Evangelists (WE)</row>
  <row label="r7">7. Paleo Freedom Fighters (PFF)</row>
  <row label="r8">8. Holistic Health Naturalists (HHN)</row>
  <row label="r9">9. Medical Freedom Libertarians (MFL)</row>
  <row label="r10">10. Vaccine Skeptics (VS)</row>
  <row label="r11">11. Universal Care Progressives (UCP)</row>
  <row label="r12">12. Faith and Justice Progressives (FJP)</row>
  <row label="r13">13. Health Care Protectionists (HCP)</row>
  <row label="r14">14. Health Abundance Democrats (HAD)</row>
  <row label="r15">15. Health Care Incrementalists (HCI)</row>
  <row label="r16">16. Global Health Institutionalists (GHI)</row>
</radio>
<suspend/>


</survey>
